# 业务配置六个子菜单原型还原与功能验收

- 验收日期：2026-08-26
- 原型入口：`http://127.0.0.1:4178/app/index.html#workflow`
- 实现入口：`http://127.0.0.1:4177/app/index.html#/workflow`
- 设计依据：仓库原型页面、PRD FR-062 至 FR-066 / FR-125、`output/business-config-reference` 桌面与移动端参考图

## 页面与功能契约

| 二级菜单 | 已实现的原型核心能力 | 数据落点 |
| --- | --- | --- |
| 流程设计 | 节点组件、可视化画布、迁移条件、责任角色、终态、签署/审计保护、退回补偿、时限升级、逐步仿真 | `config_item` / `config_item_revision` / 审计 / Outbox |
| 能力包 | 包继承、模块勾选、依赖解析、互斥冲突、受保护模块、科室覆盖、能力评级、发布任务、灰度状态机 | `capability_pack` / `capability_pack_release` / `CAPABILITY_PACK_COMPOSITION` |
| 科室适配 | 8 维覆盖矩阵、支持等级、证据哈希、专科包、有效期、缺失安全门、自动降级、科室隔离 | `department_support_assessment` / `specialty_pack_release` |
| 表单模板 | 字段组件、分组布局、字段属性、计算/校验、术语映射、签名与保护字段、样例填充、历史版本 | `FORM_TEMPLATE` 版本化 JSONB |
| 规则时限 | 平台硬门/机构硬规则/提醒/AI 建议分层、优先级、条件、处置、证据、例外、合成病例回放 | `RULE` 版本化 JSONB |
| 职责范围 | 角色×资源×动作×范围矩阵、ALLOW/DENY、临时授权、职责分离、拒绝优先、权限模拟 | `SCOPE` 版本化 JSONB |

## 视觉与交互 QA

- 保留原型既有的深色侧栏、白色顶栏、业务配置二级导航、蓝色主操作、浅灰工作区和小圆角卡片体系。
- 将旧版通用左右 CRUD 卡片替换为原型所需的设计器结构：左侧组件/台账、中部画布或矩阵、右侧属性/证据面板。
- 浏览器实测：流程节点由 6 增至 7；流程仿真回放 6 步；表单字段由 6 增至 7；规则由 4 增至 5；授权由 5 增至 6。
- 能力包实测：心血管专科包同时选择旧版全量导出后，显示互斥冲突并将评级切换为“阻断发布”。
- 科室适配实测：4 个专科范围、8 个覆盖维度；儿科显示 `PEDIATRIC_DRUG_CATALOG` 与 `GROWTH_STANDARD` 阻断项和 4 个待补维度。
- 当前应用内浏览器登录会话在后端重启后失效，因此浏览器使用明确标记的确定性仿真回退继续验证界面；数据库读写与失败关闭由后端集成测试覆盖。

## 后端、数据库与数据 QA

- `ConfigurationService` 已增加流程、表单、规则、范围、能力包组合的领域级校验，不再只检查必填字段。
- 新增的失败关闭用例覆盖：流程无终态/缺签署审计、AI 建议直接阻断、无范围永久高权。
- 仿真数据已升级到 Schema v2，包含真实结构的节点/边、表单字段、分层规则、权限矩阵、能力模块依赖与科室证据；全部使用 `SYN-*` 和明确合成身份。
- 既有测试残留清理脚本继续保留数据库名称保护，随机测试数据不混入业务台账。

## 自动化结果

- 前端生产构建：通过（Vue 类型检查、无 React 门禁、Vite 构建）。
- 后端 `ConfigurationApiTest`：6 项通过。
- 浏览器主路径：六个路由全部加载，设计器新增、切换、仿真、冲突与证据状态均通过。

final result: passed

---

# 系统管理原型一比一结构还原与功能验收

- 验收日期：2026-08-26
- 原型地址：`http://127.0.0.1:4180/app/index.html#admin`
- 实现地址：`http://127.0.0.1:4178/app/index.html#/admin`
- 桌面基准：1440 × 1000，CSS viewport 1440 × 1000，DPR 1，同一管理员登录态、同一路由默认状态
- 移动端基准：390 × 844，DPR 1
- 覆盖路由：`admin`、`admin-org`、`admin-users`、`admin-roles`、`admin-permissions`、`admin-auth`、`admin-dictionaries`、`admin-master-data`、`admin-templates`、`admin-parameters`、`admin-jobs`、`admin-audit`

## 原型结构与实现结果

| 页面 | 原型核心结构 | 最终实现 |
| --- | --- | --- |
| 管理工作台 | 指标、模块卡、高优先级待办、发布侧栏 | 实时聚合系统管理 API、PostgreSQL 审计链 |
| 组织机构 | 左侧组织树、右侧组织清单 | 机构/院区/科室/病区树与过滤清单分离；病区选择后显示 49 张床位 |
| 用户账户 | 指标、人员清单、账户处置侧栏 | 5 名中英文人员、账号、角色任期和处置动作联动 |
| 角色工作组 | 角色清单、权限风险侧栏 | 数据库有效角色任期、职责分离扫描和角色风险联动 |
| 权限策略 | 策略目录、访问模拟器 | 已发布/草稿策略、真实用户与角色选择、运行时决策器 |
| 认证安全 | 安全设置网格、高风险账户侧栏 | 版本化认证参数、登录审计、账户安全复核入口 |
| 字典术语 | 字典目录、发布值集详情 | 5 个真实字典目录、条目主从联动、检索与停用 |
| 主数据 | 领域卡、冲突清单、保护字段侧栏 | 5 类主数据版本、结构化字段和完整生命周期入口 |
| 模板输出 | 模板清单、输出预览 | 19 个数据库模板、预览、派生版本和生命周期编辑器 |
| 参数开关 | 参数清单、生效解析侧栏 | 14 个参数版本、继承解析和审批发布入口 |
| 通知任务 | 任务清单、任务解析侧栏 | 5 个任务定义、调度/批次数据和版本生命周期 |
| 管理审计 | 审计指标、事件清单、说明侧栏 | 500 条哈希链事件、检索、证据包导出与权限复核跳转 |

## 视觉与响应式证据

- 原型截图：`output/playwright/system-administration/prototype/*-1440x1000.png`
- 最终实现截图：`output/playwright/system-administration/revised/*-1440x1000.png`
- 同屏对照：`output/playwright/system-administration/comparison/*-source-vs-implementation.png`
- 移动端截图：`output/playwright/system-administration/mobile/*-390x844.png`
- 12 个桌面页面均加载对应原型骨架，页面横向溢出均为 0。
- 12 个移动页面均完成响应式检查；参数页最初有 65 px 表格溢出，改为固定表格布局与任意断词后复测为 0。
- 同屏检查重点覆盖字体层级、导航/内容列宽、卡片边框和圆角、表格密度、状态色、按钮顺序与主从详情布局；未使用占位插画、CSS 伪造图标或临时视觉资产。

## 交互与数据验证

- 工作台模块卡可以进入相应二级菜单。
- 组织树点击“心血管内科一病区”后返回 49 张床位，首条为“心血管内科-01床”。
- 用户清单从“刘畅 / Chang Liu”切换到“周明 / Ming Zhou”时，右侧账户处置详情同步更新。
- 权限模拟器加载 5 个真实用户和对应角色任期，运行结果由后端策略决策器返回 `NO_PUBLISHED_POLICY` 拒绝结果，而不是前端预设。
- 字典检索 `LAB_UNIT` 后只剩 1 个目录，右侧联动为“检验单位值集 · 当前发布值集”。
- 模板清单切换后，右侧预览从“主任医师查房记录 v1”更新为“主治医师首次查房记录 v1”。
- 审计页“进入权限复核”实际跳转角色与职责分离页面；证据包导出由当前审计事件生成 JSON 文件。
- 最终浏览器巡检未出现加载失败、不可用、`NETWORK_UNAVAILABLE` 或 Vite 错误覆盖层；前端服务终端在重启后的最终巡检中无新增代理错误。当前应用内浏览器未提供独立控制台日志能力，因此以页面错误态、资源加载结果和 Vite 代理日志共同验收。

## 数据库治理结果

| 检查项 | 结果 |
| --- | ---: |
| 有效组织/院区 `SYN` 编码 | 0 |
| `SYN` 权限策略 | 0 |
| `SYNTHETIC` 模板或文书类型 | 0 |
| 系统管理三类配置 `syn-` 键 | 0 |
| 符合“心血管内科-两位床号床”的在用床位 | 49 |
| 中英文双语有效人员 | 5 |

- 组织编码规范为 `JCUH`、院区编码为 `JCUH-MAIN`。
- 被历史审计或临床事实引用的验收人员不做物理删除，统一停用并从有效业务视图排除，以保持外键、审计链和历史语义完整。
- `scripts/clean-admin-acceptance-residue.sql` 具有开发库名称保护，并清理随机验收数据、规范业务编码和保留必要历史引用。

## 自动化验证

- 前端 Vitest：13 个文件、47 项测试全部通过。
- 前端生产构建：Vue 类型检查、无 React 依赖门禁和 Vite 构建全部通过。
- 后端：组织机构、用户任职、权限策略、配置生命周期、字典、文书模板、审计共 7 组 API/数据库集成测试通过。
- 后端 Java 编译：通过。
- 最终结果：PASSED。

## 2026-08-26 二次功能级复核（替代上述旧结论）

本轮按 `prototype/app/extensions.js` 的 12 个系统管理渲染器重新核对，不再把“有标题、有表格”视为通过。旧版截图和上述 `PASSED` 结论不再代表当前版本的最终视觉验收。

| 页面 | 本轮对齐的原型功能 | 真实数据/写入 |
| --- | --- | --- |
| 系统管理工作台 | 4 指标、8 模块入口、高优先级待办、最近发布/回退 | 聚合组织、人员、策略、配置、审计 API |
| 组织机构 | 左树右表、导入组织、变更影响、新建节点、停用 | 批量逐项写库；影响从数据库层级实时计算 |
| 用户人员 | 4 指标、批量导入、生命周期规则、人员/账号/任期主从、撤权清单 | 入职原子写入；账户停用和任期结束写库 |
| 角色工作组 | 类型筛选、职责冲突扫描、新建角色/工作组、权限风险 | `ROLE_CATALOG` 真实版本生命周期；5 条发布目录 |
| 权限策略 | 当前/草稿差异、新建策略、用户角色选择、访问模拟、紧急访问复核 | 运行时决策器；原始状态码和英文决策说明改为中文 |
| 认证安全 | 安全基线差异、安全参数网格、高风险账户、认证演练 | 版本化 `PARAMETER` + 登录审计 + 实名账户状态 |
| 字典术语 | 导入版本、引用分析、新建字典/项、目录与值集主从、排序 | 新增/停用写库；历史值不物理删除 |
| 主数据 | 7 个领域卡、同步对账、冲突/保护字段、新建变更 | 8 条版本数据，其中 1 条基线 + 7 个原型领域 |
| 模板输出 | 批量预览、新建模板/规则、预览主从、派生版本、审批发布、停用 | 文书模板不可变版本链 |
| 参数开关 | 6 项通用参数、作用域差异、来源/风险列、直接回滚、筛选排序 | 认证专用参数不再混入通用页；版本审批和回退写库 |
| 通知任务 | 通知渠道、调度策略、新建批量任务、结果/异常、责任人、差异下载 | 5 条 `JOB` 配置；渠道/责任人已写入 JSONB |
| 管理审计 | 4 指标、审计链、创建季度复核任务、导出证据包、进入逐人复核 | 复核任务写入 `JOB` 草稿；证据包来自当前审计事件 |

### 当前证据

- 真实登录 `200`，用户“林伟 / William Lin”，角色“临床医师 / 系统管理员”，退出 `204`。
- 系统管理 API：组织、人员、策略、紧急访问、模板均返回 `200`。
- 配置安全流程：无租约直连按预期返回 `403`；签发 `CONFIGURATION` 租约后，主数据、参数、任务、角色目录全部返回 `200`。
- PostgreSQL：主数据领域卡 7，通用参数 6，任务 5，发布角色/工作组目录 5。
- 前端 Vitest：13 个文件、51 项通过。
- 前端生产构建：通过。
- 后端 `ConfigurationApiTest`：通过，包含 `ROLE_CATALOG` Schema 和配置安全门。

### 视觉结论限制

本轮已使用既有原型/实现同屏对照图定位主从布局、表格溢出和控件缺失，但修正后的 12 页新截图尚未通过用户选定的应用内浏览器重拍与同屏比较。因此当前只能判定“功能端到端通过”，不应将“修正后视觉一比一”标记为通过。

final result: functional-passed / visual-recapture-pending

---

# 住院工作台九个二级菜单原型还原与 CRUD 闭环验收

- 验收日期：2026-08-30
- 参考原型：`http://127.0.0.1:4180/app/index.html#<route>`
- 当前实现：`http://127.0.0.1:4177/#/<route>`
- 桌面基准：CSS viewport 1440 × 1000，DPR 1，相同登录身份与默认业务状态
- 移动端基准：CSS viewport 390 × 844，DPR 1
- 覆盖：`inpatient`、`inpatient-overview`、`inpatient-course`、`ip-orders`、`ip-results`、`ip-consult`、`ip-pathway`、`ward`、`inpatient-discharge`

## 原型结构与业务闭环

| 二级菜单 | 原型核心结构 | 实现与真实数据作用 |
| --- | --- | --- |
| 患者列表 | 250px 病区患者列表 + 今日任务 + 315px 患者摘要右栏 | 真实在院工作清单、患者选择与上下文租约重签，文书新建/事件/审签进入正式流程 |
| 患者总览 | 临床摘要、待办和完整度右栏 | 住院天数、文书时限、完成度和业务水印均由当前住院事实计算 |
| 病历文书 | 230px 文书目录 + 文书任务主区 + 330px 质控/来源/审签右栏 | 新建任务用弹窗，文书版本只追加，分级审签与逾期事实保留 |
| 医嘱与用药 | 医嘱列表 + 用药安全/执行右栏 | 草稿新建与编辑、确定性安全预检、签署、执行、取消/停止均调用真实 API；执行事实不删除 |
| 检查检验 | 报告列表 + 异常/危急值闭环右栏 | 结果关联已完成执行；危急值接收与处置分离；删除语义为追加更正版本 |
| 查房会诊 | 会诊队列 + 证据时间轴 + 意见处理右栏 | 申请、独立医生接诊、意见签署、申请方确认和退回形成职责分离状态机 |
| 临床路径 | 230px 阶段栏 + 真实来源任务 + 330px 变异右栏 | 入径固定版本，任务由文书/医嘱状态驱动，豁免或退径须独立审核变异 |
| 护理摘要 | 床位卡 + 本班待办右栏 + 交接台账 | 真实在院清单驱动床位卡；交接新建、加入患者、完成、带原因作废全部使用弹窗和真实 API |
| 出院病案 | 出院门禁主区 + 准备度/责任链右栏 | 必需文书、岗位、并发版本由服务端重新校验；通过后床位、在院状态、审计与 Outbox 同事务变更 |

## 同屏视觉 QA

- 参考截图：`output/playwright/inpatient-full-fidelity/source-20260830/*-{1440,390}.png`
- 最终实现：`output/playwright/inpatient-full-fidelity/implementation-final-20260830/*-{1440,390}.png`
- 合并对照输入：`output/playwright/inpatient-full-fidelity/comparison-final.html` 与 `comparison-final.png`
- 九页均保留深色主导航、住院二级导航、患者条、原型主列宽度和业务责任右栏；文书、路径与护理页恢复三列骨架。
- 五个保真面：导航/信息架构、字体层级、蓝白灰与风险色、卡片/表格密度、弹窗与右栏交互均完成同屏对照。
- 实现没有复制原型在 390px 下的 64–289px 横向溢出；九页移动端 `scrollWidth === clientWidth === 390`，弹窗不越界。
- 最终实现截图的九页右栏数均为 1，桌面与移动端控制台/pageerror 均为 0。

## 交互、API 与构建证据

- `npm test -- --run src/vue/inpatient-dialog-interactions.test.ts`：1 个文件、5 项通过。
- `npm run build`：无 React 门禁、Vue 类型检查和 Vite 生产构建通过。
- `node scripts/verify-inpatient-workstation.mjs`：3/3 检查通过；9/9 路由、右栏、导航和响应式通过；8 个可变更页面弹窗通过；50 次相关 API 响应状态均为 200。
- `node scripts/verify-inpatient-crud.mjs`：医嘱“新建 → 编辑 → 安全预检/签署 → 取消”与护理交接“新建 → 带原因作废”两条真实写入闭环通过。
- 后端专项测试启动到 `compileJava`，但被工作区中另一组未完成的 `MedicalAgentHarnessController` / `MedicalAgentRunWire` 构造器参数改动阻断；该编译错误不在本次住院变更文件中。当前运行后端的两条真实写入闭环已通过。

final result: passed
