# openemr2026 交付交接（2026-08-23 UI 接线冲刺）

> 读者：接续开发 agent。开工先读 `DEVELOPMENT_PRINCIPLES.md` + `planning/2026-08-22-handover.md`。
> 关联：`planning/2026-08-23-delivery-plan.md`（计划）、`planning/2026-08-23-remaining-tasks.md`（剩余任务清单）。

## 1. 本会话完成（可用菜单 34 → 150 / 194）

主度量更新为 **150/194**（`README.md` 第 9 行已回写）。原生 Vue 纵向路由从 34 增至 150：

- **D1 治理/AI/数据（13）**：`admin-dictionaries`、`capability-pack`、`migration`、`models`、`agent-catalog`、`skill-catalog`、`tool-catalog`、`model-evaluation`、`aiops`、`data-quality`、`cohort-builder`、`research-dataset`、`opensource`。API 模块 `web/src/api/{governance,ai-platform,data}.ts`，零新后端。
- **D2 临床主链（27）**：门急诊/预约、药房/收费/检验/影像/输血/手术/护理、病案/结果/质量/病区。API 模块 `web/src/api/{emergency,execution,records,quality}.ts`。
- **D3 专科 71 路由全量（+71）**：`specialty-center` + 10 workbench + 10 record + 20 其余层（后端已就绪）+ 30 个新后端切片（V133–V162：9 qc + 4 followup + 6 care + 11 evidence/treatment）。API 模块 `web/src/api/{specialty,specialty-layers}.ts`。
- **D4 视觉对齐**：设计令牌对齐（CSS `var(--...)`）+ 状态覆盖 88/88 + 空态补齐 + `ui-audit` 文档。
- **补充 5 个纯 UI 路由**：`ai-action-review`、`ai-reminder-detail`（新增 `web/src/api/assistant.ts`）、`integration-mapping`、`specialty-coverage`、`admin-roles`。

关键改动：
- `web/src/clinical-api.ts`：导出 `request`/`adminRequest`/`issueContextLease`/`scopedHeaders`/`patientTimelineHeaders`/`inpatientHeaders`/`wardHeaders`/`explicitContextHeaders`/`orderHeaders` 供域模块复用。
- 双登记：`web/src/vue/route-registry.ts`（`nativeVueRouteIds`）+ `web/src/vue/router.ts`（`nativeComponents`）。
- 新增迁移 V133–V162（30 个专科层）+ `contracts/openapi.json` 增 schema/path + `scripts/test-schema.sh` 接线。

## 2. 关键技术点（后续开发必读）

### 2.1 上下文租约作用域（三类，前端 API 客户端必须对齐）

`security.authorize(org, facility, patient, encounter)` 要求租约 patient/encounter 与端点一致：

| 作用域 | 端点示例 | 租约 + 头 |
|---|---|---|
| 患者级（encounter=null） | pharmacy/imaging/surgery/emergency-*/medical-record-assets/action-executions | `issueContextLease(patientId, null, p)` + `explicitContextHeaders(lease, patientId, null)` |
| 患者+就诊级 | billing/lab/transfusion/vital-signs/action-approvals/clinical-reminders | `issueContextLease(patientId, encounterId, p)` + `scopedHeaders(lease)` |
| 院区级（无患者） | shift-handovers/price-catalogs/waiting-queue/specialty-support/source-* | `issueContextLease(null, null, p)` + `wardHeaders(lease)` |

### 2.2 后端切片样板（30 片已验证，照此逐片）

1. `contracts/openapi.json` 加 2 schema + 1 path（`additionalProperties:false`、snake_case）。
2. `npm --prefix contracts run generate` 生成 Java DTO + TS 契约；`check` 无漂移。
3. Java 4 件套（Service/Controller/Exception/ExceptionHandler）+ 测试。
4. 迁移 `V<n>__<table>.sql` + `assert-v<n>.sql`（约束显式命名、不可变触发器；**表名/约束/索引/触发器名须唯一，同专科多表避免 `split('_')[0]` 碰撞**）。
5. `scripts/test-schema.sh` 四处（migration_v<n> / assertions_v<n> / for 循环 / psql 链尾）。
6. 前端 list/create（双租约）+ Vue 页 + 双登记。
7. `scripts/verify.sh` 全绿 + README 回写。

### 2.3 前端契约

- 所有 list/create 走生成 DTO（`contracts.ts` 的 zod schema），禁止手写枚举。
- 写操作统一 `Idempotency-Key` + 乐观锁 `expected_row_version`。

## 3. 验证状态（150/194，全绿）

- `cd web && npm run build`：`BUILD_EXIT=0`（150 路由，无重复）。
- `scripts/verify.sh`：`VERIFY_EXIT=0`（契约 440 schemas 无漂移 + V1–V162 迁移/断言 + Java 测试 + Web 17 测试 + 构建 + 安全全绿）。
- **浏览器全路由审计**（`web/scripts/verify-browser-routes.mjs`）：194/194 verified，0 failures / 0 failedResponses / 0 consoleIssues / unknownSafe（D2 完成后运行过；已修审计脚本对「原生专科页」的判定）。
- 设计令牌：`web/src/styles.css` `:root` 全令牌 + `styles.css`/`vue-shell.css` 核心色值 `var(--...)`。
- 运行时修复项：er-record 双租约、admin-templates 日期防御、migration 必填参数、6 个 evidence/treatment 迁移索引名去重。

## 4. 运行方式

```bash
scripts/dev-db.sh status
# 后端（Spring Boot，dev-synthetic，端口 8080）——本会话已重启，加载 V1–V162
SPRING_PROFILES_ACTIVE=dev-synthetic scripts/with-java21.sh ./gradlew bootRun
# 前端（Vite，端口 4177，/api 代理到 8080）——本会话已重启
cd web && npm run dev
# 生产应用 http://127.0.0.1:4177/ （hash 路由 #/<route_id>）
```

## 5. 剩余工作

见 `planning/2026-08-23-remaining-tasks.md`：44 个 `NOT_AVAILABLE`（42 NO-GO 真实适配器 + 2 需新后端：admin-audit、ai-assistant）+ 1 项 D4 像素级高保真复核（设计/生图人工验收）。

## 6. UI 对齐高保真原型的做法与踩坑（本轮新增，必读）

> 完整 10 条根因见 `DEVELOPMENT_PRINCIPLES.md` §六。这里只留「怎么做对」的操作清单与关键文件。

**对齐原型的正确姿势（别再猜）**：
1. 以**实际渲染**为准，不以源码猜测。用 Playwright `page.evaluate` dump `getComputedStyle` + `element.textContent` 到文本（模型读不了截图），据此对齐。
2. 视觉真相是 `prototype/app/styles.css`（已演进），不是 `tokens.json`。
3. 原型导航在 `prototype/app/coverage.js`（`coveragePages`，14 项 5 组）+ `specialties.js`（`specialty-center` 拼接），**不是** `app.js` 里那份旧 `pages`。

**本轮落地文件**：
- `web/src/prototype.css` = `prototype/app/styles.css` 原样拷贝（最后 import）。
- `web/src/align-prototype.css` = 生产 `.admin-*`/`.button`/`.content` → 原型 `.card`/`.table`/`.metric`/`.status`/`.btn`/`.main` 的类名映射。
- `web/src/vue/components/ClinicalShell.vue` = 改用原型类名（`.shell`/`.sidebar`/`.main`）+ 14 项侧栏 + center-nav 子导航。

**已修的历史 bug（勿重犯）**：
- `:root` 令牌自引用（`--nav:var(--nav)`）→ 颜色全失效。
- 双层 padding（`.main` + `.content` 各 22px）→ 内容双倍缩进。
- `.nav-item > span` 边框盒残留 → 图标/文字/计数都套白线框。
- CSS 注释含 `*/`（`.admin-*/`）→ lightningcss 报错。
- vue-tsc 内存不足假挂起 → 先释放内存或用 `vite build` 验证。
