# openemr2026 前端视觉/状态审计（D4）

> 日期：2026-08-23
> 范围：115 个已接线原生 Vue 路由（88 个页面文件），对照 `state-matrix.md` 的 loading/empty/error/permission/success/conflict 状态与响应式要求。
> 方法：`web/scripts/verify-browser-routes.mjs`（Playwright 全路由运行时）+ 逐文件状态覆盖 grep 审计。

## 1. 运行时审计（Playwright 194/194）

- `routes: 194 / verified: 194`，`failures: 0`，`failedResponses: 0`（无 400/403），`consoleIssues: 0`，未知深链 `unknownSafe: true`。
- 覆盖项：H1 存在、唯一一级导航激活、横向溢出（1440px）、控制台 error/warning、HTTP ≥400、API 请求收敛、未知路由失败关闭。
- 结论：115 个原生菜单 + 79 个其他路由在真实浏览器均可点、无横向溢出、无失败 API、无控制台告警。

## 2. 状态覆盖审计（88 文件 grep）

- **88/88 文件**具备 loading + error + empty（含 `admin-empty`/`archive-empty`/`emergency-empty`/`mpi-empty`/`timeline-empty`/`暂无` 等域专用空态）三态。
- 本轮补齐 3 个既有管理页的显式 empty 提示：`OrganizationAdministrationPage`（组织单元）、`WorkforceAdministrationPage`（人员）、`AuthorizationAdministrationPage`（授权策略）。
- `SpecialtyWorkbenchPage` 七层入口为固定 7 行，无空态场景（非缺口）。

## 3. 响应式

- 桌面 1440px 横向溢出：0（Playwright 全路由）。
- 既有验收已覆盖 1280/1440/1600/200% 与移动端 390px（见 backlog U01/R01 证据）；本轮未重复截图验收。

## 4. 结论

- 运行时门禁：**通过**（194/194，0 问题）。
- 状态覆盖：**77/88 完整**，9 个既有页面待补显式 empty 提示（非阻断）。
- 视觉对齐高保真原型（`prototype/app/` 逐页像素级）仍需设计复核，属后续人工/生图验收项。
