# R01-T1 病历模板全生命周期验证报告

> 日期：2026-08-20  
> 范围：FR-008，并将模板必填规则接入 FR-009/010 质控与签署门禁  
> 结论：`LOCAL_VERIFIED`；R01 总任务仍为 `IN_PROGRESS`

## 交付结果

- V30 建立租户内模板、不可变版本、通用/机构/院区/科室范围、生效期和发布唯一性约束。
- 已发布和已退役版本的结构、必填、显示规则、创建人和审批证据不可修改；创建人不能审批自己的版本。
- 每份病历创建时绑定唯一 `template_version_id`；模板升级只影响之后新建病历，历史病历保留原版本和原语义。
- 模板 `required_fields` 已进入确定性质控，缺失项产生 `BLOCKING` 问题；质控运行证据固化模板版本水印，不通过时无法签署。
- 管理 API 完成列表、创建 v1 草案、派生新版、独立审批发布和停用；命令带幂等、审计链与 Outbox。
- `#/admin-templates` 为原生 Vue 页面，呈现范围、版本、字段、必填、生效和审批证据，并可执行上述真实 API。

## 关键验证

| 门禁 | 结果 |
|---|---|
| `DocumentTemplateApiTest` | PASS：自批被拒绝，独立发布，v1/v2 分别绑定，历史语义稳定，必填缺失阻断，停用后不得新建 |
| `ClinicalLifecycleApiTest` | PASS：历史基线模板兼容，质控、签署、内容不可变回归通过 |
| `scripts/verify.sh` | PASS：115 schemas / 123 outputs / 92 operations，100 AI eval，15 安全载荷，V1–V30 迁移与隔离恢复，30 suites / 75 Java tests，Web 13 tests，Vue 生产构建，安全扫描与 194 路由制品审计通过 |
| Chromium 全路由审计 | PASS：194/194，无 H1/一级导航/横溢出/控制台/HTTP 失败，未知深链失败关闭 |

## 未扩大声明

- 本切片不等于 R01 完成；文书附件、自动暂存/离线恢复、并发差异恢复、更正链和真实 CA 仍待收口。
- 科室范围数据已可配置，但就诊科室的运行时解析仍需在 R01/C01 后续切片补齐，不将“可存储”宣称为“已全面生效”。
- 远端 CI、真实医院数据和真实多管理员 IdP 联调尚未执行。
