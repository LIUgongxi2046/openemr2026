# OpenEMR2026 PC 全功能、多角色链路与 S010 安全验收报告

- 执行日期：2026-08-31（Asia/Shanghai）
- 测试冻结提交：`c50c7f880b6bfefb8181fc6df9f0f75cb761f29d`
- 分支：`main`
- 范围：PC Web（基线视口 1440×1000）、Java API、PostgreSQL 18.4、合成数据、AI Evals、低影响安全验证
- 明确排除：移动端不纳入本轮验收分母与发布门禁
- 环境：`dev-synthetic`，数据集 `s009-comprehensive-v1`，seed `20260824`，10 类 persona，32 个合成患者画像
- 总体结论：**NO_GO**

## 1. 结论摘要

工程基线、合同、数据库、API、门诊主体页面、急诊、住院、质量中心及本地安全控制的大部分证据通过。三轮 PC 多角色链路均能稳定穿越门诊、急诊、住院、质控和系统管理域。

但严格发布门禁不能通过，原因是：

1. Java 全量回归 714 项中仍有 2 项稳定失败（业务配置导入期望与当前数据口径不一致）。
2. PC 预约挂号完整写链未跑通：班次选择控件不可见，班次号源配置页标题契约不匹配，入院页缺少验收要求的“入院途径”。
3. 全局 AI 医助对话输入框没有可访问名称，严格 UI 审计无法通过 `getByLabel`；AI 中心另有首个“编辑”按钮禁用导致的自动化契约失败。
4. 产品顶栏仅有临床医生、住院医生、质控管理员、系统管理员 4 个可切换角色；护士和药师无独立登录/角色入口，因而无法完成真实的职责分离用户验收。
5. DeepSeek 真实模型红队因模型制品、SHA-256、推理引擎、硬件和批准阈值未配置，门禁为 `INTEGRATED_BLOCKED`。

## 2. 工程与合同证据

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 测试前代码冻结 | PASS | 提交 `c50c7f8`，458 文件，+38041/-6931；提交后开始验收 |
| Contracts 单测 | PASS | 4/4 |
| Contracts 生成检查 | PASS | 502 schemas，510 outputs |
| 需求可追溯性 | PASS | FR 138/138，AC 138/138，route refs 138/138 |
| 路由图 | PASS | 194/194 路由，3 个资产，205 个链接 |
| 语义合同 | PASS | 194/194，其中 36 条高风险路由 |
| API 静态审计 | PASS | 488/488 |
| API 在线审计 | PASS | 488/488 在当前后端和合成 DB 上执行 |
| Web 单测 | PASS | 26 文件，131/131 |
| Web 生产构建 | PASS with warning | 构建通过；主 bundle 524.00 kB（gzip 105.90 kB）超过 500 kB 提示线 |
| DB 迁移 | PASS | V1–V200 事务迁移、约束和回滚通过 |
| 备份/恢复 | PASS | 恢复后指纹一致 |
| Java 全量测试 | FAIL | 714 项：707 通过，7 失败 |

Java 7 项失败的归因：

- 5 项 `ProdSecurityApiTest` 首轮因 `openemr2026.archive.storage-root` 缺失而上下文启动失败；补充临时安全目录后 5/5 通过，归类为环境配置。
- 2 项 `TertiaryBusinessConfigurationImportTest` 定向复跑仍失败。实际汇总为 `models=1, skills=24, tools=28, evaluations=5`，测试仍期望 `models=6, skills=24, tools=24, evaluations=10`。这是当前数据/测试口径的稳定不一致，不是偶发错误。
- 编译另有 5 条 Jackson 过时 API 警告，归为 P2 工程债。

## 3. PC 功能验收

### 3.1 已通过

| 业务域 | 结果 | 覆盖 |
| --- | --- | --- |
| 门诊 | PASS | 37/37；门诊工作台、病历、诊断、医嘱处方、结果、会诊转诊、随访终诊及 PC 布局 |
| 急诊 | PASS | PC 6/6 路由；新增/编辑/删除弹窗；高危护理记录新建→更正版本→逻辑删除；48 次 API 仅 200/201 |
| 住院嵌套业务 | PASS | PC 5/5 入口；住院药房新增→编辑→作废；48 次 API 仅 200/201 |
| 住院医嘱/病区 | PASS | 医嘱新建→编辑→签署→取消；病区记录新建→作废 |
| 住院工作站 | PASS | PC 9/9 路由、真实数据标识、8 个业务变更入口弹窗；50 次 API 均 200 |
| 质量中心 | PASS | 5/5；质量项目、科室缺陷、评级证据、院感确认/排除、资质创建/更新/撤销；21 次写请求全部成功 |
| 病历中心导航 | PASS | 9 项；PC 导航位置/名称及 7 条子路由 |
| 登录/退出 | PASS | 独立登录页、DB 登录、退出后会话失效 |
| 未知深链 | PASS | 认证后显示 404 及“页面不存在或尚未登记”，不静默回落 |

急诊脚本唯一的非产品失败是 `http://127.0.0.1:4180` 原型参考站未启动；PC 实现本身的 4 个核心检查全部通过。

### 3.2 未通过或证据不足

| 等级 | 问题 | 分类 | 处置建议 |
| --- | --- | --- | --- |
| P1 | 预约挂号完整写链在“可预约班次”选择处超时 | 产品/契约待联合定位 | 确认选择器实际可见性、弹窗打开时机和号源数据，修复后重跑预约→报到→叫号→接诊→退号 |
| P1 | 班次号源配置标题契约失配，入院页缺“入院途径” | 产品/验收契约 | 由产品与 S008 确认 PRD 真值后统一 UI 与测试 |
| P1 | AI 医助 composer `<textarea>` 仅有 placeholder，无 label/`aria-label` | 产品可访问性 | 增加稳定可访问名称，同步更新严格 UI 脚本 |
| P1 | 护士/药师无独立可切换角色 | 产品覆盖缺口 | 增加服务端可校验的独立测试账号和角色任期，重跑职责分离链路 |
| P1 | 系统管理 CRUD 脚本 2 通过/6 失败 | 主要是测试契约漂移 | 当前页已将“认证场景演练”改为“打开账户安全复核”，并改了标题/提示；先更新验收契约再判定剩余产品问题 |
| P1 | 病历写入脚本 0/2 | 测试契约漂移 | 脚本等待已不存在的 `.record-prototype-metrics`，当前实现为 `.record-metrics`；更新脚本后重跑写入生命周期 |
| P1 | 病历编辑/作废弹窗无可用草稿 | 数据前置/测试契约 | 为脚本建立可编辑草稿前置，不应直接选取被禁用操作 |
| P1 | 病案资产 UI 写入脚本未观测到预期 POST | 测试工具/产品待定位 | 脚本的延时 Promise 导致未捕获报告就崩溃；修复 harness 后重跑扫描→OCR→编目→验真→复制→借阅→归还→WORM |
| P2 | 患者切换脚本使用旧 `.queue-patient` 前置 | 测试契约漂移 | 改为当前候诊列表 DOM/可访问名称 |
| P2 | 全路由数据、按钮、一致性、顶栏脚本缺登录/会话续期 | 测试基础设施 | 统一复用登录 storage state，在长跑中续期；本轮已终止会产生大量误报的运行 |

## 4. 多角色诊疗链路

使用当前产品实际存在的 4 个 PC 角色，连续执行 3 轮。每轮 26 个节点，共 78 个角色—页面节点，全部路由 ID、H1 与错误态检查通过。

1. 临床医生：预约挂号 → 门诊工作台 → 门诊病历 → 诊断 → 医嘱 → 检查检验 → 随访终诊。
2. 住院医生：急诊工作台 → 预检分诊 → 急诊病历 → 急诊护理 → 交接 → 入院床位 → 住院工作站 → 住院医嘱 → 病区护理 → 出院归档。
3. 质控管理员：全院病历 → 病历质控审签 → 医疗质量中心。
4. 系统管理员：系统管理 → 认证与账户安全策略。

这 3 轮证明的是 PC 角色上下文切换、跨域路由、数据展示和错误态稳定性。写入闭环由门诊、急诊、住院和质量中心的专项脚本提供。由于顶栏角色只是 4 个可见上下文，且没有护士/药师独立账号，本轮不把它表述为完整 RBAC/职责分离验收。

## 5. S010 安全工程与渗透结果

### 5.1 授权与方法

- 模式：`THREAT_MODEL + ASSESS + EXECUTE`
- 资产：合成患者数据、身份/角色、上下文租约、诊疗写入/签名、审计/Outbox、病案对象、AI 模型/工具副作用。
- 信任边界：浏览器↔会话/API、API↔PostgreSQL、API↔对象存储、API↔模型/工具、租户与患者/就诊上下文。
- 主要威胁：未授权/IDOR、伪造租户、SQL 注入、XSS、跨患者泄露、提示词注入、未授权工具副作用、证据篡改、秘密泄漏。
- 方法限制：只对本机合成环境进行低频、低影响请求；未做 DoS、密码爆破、数据外传、社会工程、生产/第三方测试。

### 5.2 结果

| 安全项 | 结果 | 证据 |
| --- | --- | --- |
| 红队数据集完整性 | PASS | 18 个合成载荷，15 类攻击面 |
| 安全静态门禁 | PASS | 凭据模式、生产 bundle 合成身份泄漏、内联秘密、prod/dev 隔离全通过 |
| Java 安全集成测试 | PASS | 38/38，0 failures/errors |
| 未认证与伪造 Bearer | PASS | 均返回 401 |
| 安全响应头 | PASS | `X-Content-Type-Options: nosniff`、`X-Frame-Options: DENY`、`Cache-Control: no-store` |
| SQL 注入 | PASS | `%' OR 1=1; DROP TABLE patient; --` 返回 200/0 行，无副作用 |
| XSS 反射 | PASS | 活性 `<img onerror>` 载荷未以标记反射 |
| 伪造租户头 | PASS | 有效会话的服务端作用域未被伪造 header 切换 |
| 危险 HTTP 方法 | PASS | TRACE 返回 405 |
| npm 生产依赖离线审计 | PASS | 0 vulnerabilities |
| DeepSeek 真实模型红队 | BLOCKED | 集成文件完整，但制品/引擎/硬件/批准阈值缺失 |

S010 本地低影响安全结论为 **LOCAL PASS WITH RESIDUAL RISK**：未发现可复现的高危漏洞，但不能替代真实 IdP、真实对象存储、真实 DeepSeek 推理环境和生产部署的授权安全评估。

## 6. 发布门禁与修复顺序

建议按以下顺序回到 S008 修复，然后定向重跑 S009/S010：

1. 修复预约挂号→报到→叫号→接诊→退号的 PC 完整写链，并核对班次配置和入院字段真值。
2. 为 AI 医助 composer 增加 label/`aria-label`，并让严格 UI 脚本按当前 Eva 交互契约执行。
3. 确定业务配置导入的最终数量口径，使 `TertiaryBusinessConfigurationImportTest` 零失败。
4. 修复全路由/按钮/一致性/顶栏/病历/病案/系统管理的自动化登录、选择器、前置数据和超时逻辑。
5. 增加护士与药师的独立测试账号/服务端角色任期，完成真实 RBAC 和职责分离链路。
6. 提供可校验的 DeepSeek 模型制品、硬件、推理引擎与批准阈值，再执行真实 AI 安全红队。

重跑通过条件：Java 全量零失败；PC 核心写链零 P0/P1；自动化工具无登录/契约误报；护士/药师角色链路可验证；真实模型安全门禁不再 `BLOCKED`。

## 7. 证据位置

- 本轮原始日志、多角色脚本、S010 探针与截图：`artifacts/test-runs/s009-full-20260831-c50c7f8/`
- Playwright 结构化结果与截图：`output/playwright/`
- Java 测试 HTML：`build/reports/tests/test/index.html`
- Java 安全测试 XML：`build/test-results/test/TEST-*.xml`

测试产物是在冻结提交之后生成的未提交证据；本轮未自动修改业务代码，也未创建第二个“测试后”代码提交。
