# openemr2026 开发方向纠偏与 UI 接线冲刺计划

> 日期：2026-08-22
> 性质：对「先完成全局、再动专科」执行期的一次**根因复盘 + 计划纠偏**
> 关联：`docs/process/planning/2026-08-14-openemr2026-v1.0-implementation-backlog.md`（权威 backlog）、`docs/process/planning/2026-08-22-handover.md`

---

## 1. 根因（务必读透，别再犯）

**不是技术栈/脚手架问题。** Vue3 + Vite + Router + Pinia + TanStack Query + 自动生成契约 + 194 路由注册表**早已就位**。

真正的根因是：**「完成」的定义和「进度」的度量都被设成了后端指标，导致所有 agent 把 token 花在"看不见的地基"上，把"看得见的菜单"系统性后置。**

三个机制性错误：

1. **DoD 写反了**：backlog 写「不把 PRD/原型/静态 UI 计为已实现」。副作用 = UI 成了"不计分"的活，agent 永远不做 UI。
2. **度量是纯后端的**：机器契约记 `schemas/outputs/docs/process/operations/tests`，从无「可用菜单数」。测什么就优化什么。
3. **纵向切片被做成横向层**：真纵向 = UI→API→DB 端到端；实际 = 一张表+服务+API+测试（后端横切一层），UI 层始终缺。

**Agent 盲区**：agent 按「verify.sh 全绿 = 完成」汇报，而 verify 是后端/测试门禁，不测"人能不能点"。Agent 没有"这不能用"的人类直觉。

## 2. 结论：继续，不推翻重来

后端（132 迁移 + 477 测试）是真实资产，推翻 = 把做对的也丢掉。缺的只是 UI 层，是**加法**。

## 3. 纠偏：换三样东西

| 原来（错） | 改成（对） |
|---|---|
| 度量 schemas/docs/process/operations/tests | **主度量 = 可用菜单数 / 194** |
| DoD = 后端切片 + 测试绿 | **DoD = 菜单在 UI 可点、连真 API、状态齐全** |
| 任务 = 一个领域一张表 | **任务 = 一个菜单端到端（UI→API→DB）** |

### 统一"菜单切片"交付模板（替代原后端切片模板）
每个菜单一个任务，完成后必须：
1. `web/src/vue/views/<Xxx>.vue` 页面，**视觉对齐原型**（参考 `prototype/app/`、`docs/design/ui-delivery/tokens.json`）。
2. `router.ts` 的 `nativeComponents` + `route-registry.ts` 的 `nativeVueRouteIds` 双登记。
3. 复用 `web/src/clinical-api.ts` 既有 API 函数（有则复用，无则新增，**禁止再新建后端表/服务，除非该菜单真的没有后端**）。
4. 覆盖状态：loading / empty / error / permission / success / conflict（用 `ClinicalPageState.vue` + `toClinicalIssue`）。
5. 验收：`cd web && npm run build`（vue-tsc 通过）+ 浏览器打开 `#/<route_id>` 真实可点。
6. 回写：README 第 5 行把「可用菜单数 / 194」更新为**主指标**。

## 4. 四阶段执行清单

### P0 — UI 接线冲刺（零新后端，最高 ROI）
把已就绪后端的菜单逐个接成页面。后端已就绪、待接 UI 的清单（route_id）：

| 菜单 | 后端 |
|---|---|
| `admin-dictionaries` 字典/术语/值集 | V51 DictionaryService |
| `capability-pack` 机构能力包与继承 | V82 + V121 灰度发布 |
| `models` 基座模型目录 | V52 ModelDeploymentService |
| `agent-catalog` Agent 目录 | V80 |
| `skill-catalog` Skill 目录 | V84 |
| `tool-catalog` Tool 目录 | V85 |
| `model-evaluation` 模型评估 | V87 |
| `aiops` AI 运行/预算/事件 | V88 + V132 预算强制 |
| `data-quality` 数据质量 | V77 + V93 |
| `cohort-builder` 科研队列 | V81 + V95 + V127 成员 |
| `research-dataset` 科研数据申请 | V53 |
| `opensource` 开源指标 | V110 + V123 下载事件 |
| `migration` 历史迁移与切换 | V115 + V126 + V129 + V130 + V131 |

> 每个菜单都有完整后端，纯 UI 接线即可，是最快把 token 变成屏幕内容的一批。

### P1 — 临床主链缺口菜单
`医疗协同执行` 缺 12、`病历与病案` 缺 9、`临床工作域` 缺 13 里，先补有后端/复用后端的（药房/收费/检验/急诊/护理/转诊/不良事件/院感等），再补需新后端的。

### P2 — 专科 71 菜单
S01–S10 剩余数据层（约 17 片）+ 71 个专科深页 UI。

### P3 — 视觉对齐
把现有 33 个"功能壳"页面，对照原型 `prototype/app/` 高保真 + `docs/design/ui-delivery/tokens.json` 逐页做视觉/交互对齐。

## 5. 给下一个 agent 的最小上下文
- 本文件（根因 + 菜单切片模板 + 四阶段）。
- `docs/process/planning/2026-08-22-handover.md`（纪律/踩坑/文件位置）。
- `contracts/generated/route-contract.generated.json`（194 路由的 route_id/title/primary_domain）。
- `web/src/vue/route-registry.ts`（nativeVueRouteIds 就是"已可用菜单"白名单）。
- `web/src/vue/views/OrganizationAdministrationPage.vue`（现有页面的写法样例）。
- **硬约束：优先接 UI，禁止再堆后端切片。**
