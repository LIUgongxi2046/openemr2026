# openemr2026 开发交接文档

> 日期：2026-08-22（含复盘更新）
> 目标读者：接续开发的 agent / 工程师
> **开工第一步：先读 [`DEVELOPMENT_PRINCIPLES.md`](../../../DEVELOPMENT_PRINCIPLES.md)（19 条硬原则），再读本交接。**
> 关联：`docs/process/planning/2026-08-14-...-backlog.md`（权威 backlog）、`docs/process/planning/2026-08-22-ui-replan.md`（UI 接线冲刺计划）、`scripts/verify.sh`（全量门禁）

---

## 0. 最重要的三句话（先读这个）

1. **方向已纠偏**：此前「后端优先、UI 后置」是错的。现在 **"完成" = 菜单在 UI 可点**，主度量 = **可用菜单数 / 194**，不再用 schemas/tests 当进度。
2. **不推翻重来**：后端 132 迁移 + 477 测试是真实资产；缺的是 UI 层，是**加法**。但 **UI 优先，后端复用，禁止再堆后端切片**。
3. **架构不用大动**：模块化单体 + PostgreSQL + 显式 SQL(JdbcClient) + Vue 单栈 + 契约优先 = 合理甚至优秀。**禁止换 MyBatis-Plus/JPA、禁止换 MySQL、禁止微服务化、禁止追 Spring Boot 新大版。**

## 1. 现状快照（截至本交接）

- **机器契约**：`380 schemas / 388 outputs / 356 operations`，数据库迁移 **V1–V132**。
- **测试**：`119 suites / 477 tests / 0 failures`；`100/100` AI eval；`15 payloads / 12 surfaces` 红队；`138/138` FR 追踪；`194/194` 路由审计。
- **门禁**：`scripts/verify.sh` 全绿（`VERIFY_EXIT=0`）。
- **前端**：Vue 3 单栈；`web test + build` 通过（`17` 个前端测试）。
- **UI 现实（关键）**：194 路由里**只有 33 个是原生 Vue 页面**（VUE_NATIVE），71 个专科是"支持守卫"（读了支持声明默认拒绝），90 个 `NOT_AVAILABLE`。**这是当前最大缺口。**

## 2. 本地运行

```bash
scripts/dev-db.sh status      # PostgreSQL 18，55432，dev 库 openemr2026_dev
# 后端（Spring Boot，dev-synthetic profile，端口 8080）
SPRING_PROFILES_ACTIVE=dev-synthetic scripts/with-java21.sh ./gradlew bootRun
# 前端（Vite，端口 4177，/api 代理到 8080）
cd web && npm run dev
# 原型（静态高保真设计稿，端口 4178）
cd prototype && python3 -m http.server 4178 --bind 127.0.0.1
```

- 生产应用：http://127.0.0.1:4177/ （`#/record` 等已实现路由）
- 高保真原型：http://127.0.0.1:4178/prototype.html （成品该长这样）
- 就绪探针：`GET /api/v1/system/readiness`

## 3. 本次会话完成的工作（迁移切片清单，V120–V132）

| 迁移 | 史诗 | 切片 | 硬门要点 |
|---|---|---|---|
| V120 | S06 眼科（专科收尾） | 术后随访 | 术后高眼压必填并发症 |
| V121 | G01 | 能力包灰度发布/回滚 | 同包单 ACTIVE、状态机、回退必附原因 |
| V122 | O01 | 科室级任务队列/团队视图 | 终态拒入队、团队成员领取 |
| V123 | D01 | 开源下载事件 | 服务端机器人判定、渠道+指纹去重 |
| V124 | A02 | 动作执行核验 | 仅已批准可执行、失败必附原因 |
| V125 | O01 | 定时投递调度 | 到期 PENDING 批量投递、SKIP LOCKED |
| V126 | Q01 | 源系统盘点 | 编码唯一、生命周期状态机 |
| V127 | D01 | 队列成员物化 | 活动队列/活动患者、同队列去重 |
| V128 | A02 | 提醒转任务/限频 | clinical_task 增 REMINDER 源类型、一提醒一任务 |
| V129 | Q01 | 断点重跑 | 可续迁批次、进度单调 |
| V130 | Q01 | 源字段映射 | 仅已配置源、映射唯一 |
| V131 | Q01 | 迁移源患者匹配 | 确定性匹配、复核硬门 |
| V132 | A01 | 运行预算强制校验 | 累计 token/时长不超限 |

> ⚠️ 上述 13 片**全是后端切片，零 UI**——这正是方向纠偏的直接诱因。每片证据报告在 `docs/process/testing/2026-08-22-<epic>-<slice>-report.md`。

**按新原则交付的第一个 UI**：`#/admin` 系统管理工作台（`web/src/vue/views/AdminWorkspacePage.vue`），从 NOT_AVAILABLE 变为真实可用，登记在 `router.ts` 的 `nativeComponents` + `route-registry.ts` 的 `nativeVueRouteIds`。

## 4. 既定工程纪律（每片务必照做）

> 完整 19 条原则见 `DEVELOPMENT_PRINCIPLES.md`；这里只留"怎么做对"的操作细则。

**后端切片（仅在确实需要新后端时）** = 迁移 + 模块 + 契约 + 测试 + 全量 verify + 报告 + 台账回写：
1. **迁移**：`V<n>__<name>.sql` + `assert-v<n>.sql`；约束**显式命名**；**绝不可改已应用迁移**（checksum），修复只能新迁移。
2. **接线 test-schema.sh**：`migration_v<n>` + `assertions_v<n>` 变量 + required-file 循环项 + psql 链尾行（`-c 'rollback'`），四处都改。
3. **契约**：先改 `contracts/openapi.json`（`additionalProperties:false`、snake_case），再 `npm --prefix contracts run generate`。**新表名/`$ref`/`operationId`/路由路径必须先 `grep` 全仓查重**。
4. **模块**：`Service/Controller/Exception/ExceptionHandler` + 1 测试；写操作统一 `beginCommand`(幂等) + `appendEvidence`(审计链+outbox) + `completeCommand`；乐观锁 `expected_row_version` + `for update`。
5. **测试**：`@SpringBootTest(RANDOM_PORT) @ActiveProfiles("dev-synthetic")`；种子 `TENANT/ORGANIZATION/FACILITY/USER/ROLE`（`018f0000-...aa01..aa05`）；**无 `@Transactional`，数据跨 run 持久化** → UUID 派生唯一键、计数用差值断言；可空列用 `cast(:x as uuid/timestamptz)` + 传 `UUID/OffsetDateTime`（pgjdbc 不认 `Instant`）。
6. **验证**：后台 `scripts/verify.sh > /tmp/verify-v<n>.log 2>&1; echo "VERIFY_EXIT=$?"`，`job_output wait:true`（勿 `| tail` 吞退出码）。

**菜单切片（当前主流，优先做这个）** = 见 `docs/process/planning/2026-08-22-ui-replan.md` §3 模板：Vue 页面视觉对齐原型 → `router.ts` + `route-registry.ts` 双登记 → 复用 `clinical-api.ts` 既有 API → 覆盖 loading/empty/error/权限/success → `npm run build` 通过 → 更新"可用菜单数/194"主指标。

## 5. 剩余工作（按新优先级）

### 5.0 P0 — UI 接线冲刺（零新后端，最高 ROI，**当前就该做这个**）
把已就绪后端的菜单逐个接成页面（详见 `ui-replan.md` §4）：`admin-dictionaries` 字典、`capability-pack` 能力包+灰度、`models` 模型、`agent-catalog`/`skill-catalog`/`tool-catalog` 目录、`model-evaluation`、`aiops` 预算、`data-quality`、`cohort-builder`、`research-dataset`、`opensource`、`migration` 迁移。

### 5.1 P1 — 临床主链缺口菜单
协同执行缺 12、病历病案缺 9、临床工作域缺 13（优先复用后端的药房/收费/检验/急诊/护理/转诊/不良事件/院感）。

### 5.2 P2 — 专科 71 菜单 + 剩余数据层
S01–S10：S01 care；S02 evidence/qc；S03 evidence/treatment/qc；S04 followup/qc；S05 treatment/evidence/qc；S06 evidence/qc；S07 treatment/evidence/followup/qc；S08 care/evidence/followup/qc；S09 care/evidence/qc；S10 care/followup/qc；+ 10 科 workbench UI。

### 5.3 P3 — 视觉对齐
把现有 33 个"功能壳"页面按原型 `prototype/app/` + `docs/design/ui-delivery/tokens.json` 逐页对齐高保真。

### 5.4 全局残留（低优先级）
G01 可视化配置（UI 重）；A01 审批流编排（与 A02 单步审批重叠，多步属重设计）；A01 SSE 断点恢复（真实流通道）。

### 5.5 NO-GO 真实适配器（待授权）
OIDC/CA/KMS/LIS/PACS/对象存储/真实药典/HR-IAM/真实下载采集器/真实消息通道/真实模型推理计量。

## 6. 已查明的「伪剩余项」（别再重复造）
- **G01 继承环检测**：schema 已结构保证（唯一 code + 非延迟外键 + 不可变触发器 ⇒ 恒为 DAG）。
- **Q01 评级取证**：V6 `SpecialtySupportService` 已强制证据哈希 + 无缺失安全门 + 未来到期。
- **Q01 源盘点/源映射/断点重跑/患者匹配**：V126/V130/V129/V131 已落地。

## 7. 架构决策结论（本轮复盘定案，别再摇摆）
- **继续，不推翻重来**。后端是资产，UI 是加法。
- **保留 JdbcClient**，禁止换 MyBatis-Plus/JPA/Hibernate（会重写 102 个 Service + 丢 DB 约束优势）。
- **保留 PostgreSQL**（医疗严谨性 + 信创国产库 PG 兼容），不换 MySQL/Oracle。
- **Spring Boot 4.1.0 可选降 3.5.x**（求稳/招人/集成；非必须，代码已在 4.1 全绿）。
- **面向中国信创**：目标库金仓/openGauss/GaussDB/达梦 PG 兼容模式；可引入 Element Plus/Knife4j（纯加法）。
- 详见 `DEVELOPMENT_PRINCIPLES.md` §二/§三。

## 8. 踩坑记录（后续开发必读）
1. **表名/契约名冲突**：V131 最初叫 `patient_match_candidate` 与 V29 MPI 的 `patient_match_candidate` 重名 → 字段字典重复 + Spring 路由歧义。**新表名/`$ref`/`operationId` 先 `grep` 查重。**
2. **pgjdbc 不认 `Instant`**：可空 timestamptz 用 `cast(? as timestamptz)` + 传 `OffsetDateTime`。
3. **可空列类型推断**：`values(..., ?, ?)` 两个 `null` 报「could not determine data type」，用 `cast(:x as uuid/timestamptz)` 显式定型。
4. **测试数据跨 run 持久化**：计数/去重断言用差值或 UUID 派生唯一键。
5. **`OutboxDispatcherTest` 并发抖动**：outbox 积压 > 领取批量导致事件不被优先领取；把用例事件 `available_at` 置最旧。
6. **`Instant.now().atOffset(ZoneOffset.UTC)`**：timestamptz 统一走 `OffsetDateTime`。
7. **字段字典唯一键**：`generate.mjs` 用 `entity.field` 做唯一键，同名表会静默重复。

## 9. 关键文件位置
- **开发原则（必读）**：`DEVELOPMENT_PRINCIPLES.md`
- **UI 冲刺计划**：`docs/process/planning/2026-08-22-ui-replan.md`
- 权威 backlog：`docs/process/planning/2026-08-14-openemr2026-v1.0-implementation-backlog.md`
- 机器契约单行：`README.md` 第 5 行
- 全量门禁：`scripts/verify.sh`
- 迁移：`src/main/resources/db/migration/V1__*.sql` … `V132__*.sql`；断言：`src/test/resources/schema/assert-v1..v132.sql`
- 契约源：`contracts/openapi.json`；生成器 `contracts/generate.mjs`；治理源 `contracts/governance.source.json`
- 路由白名单：`web/src/vue/route-registry.ts`（`nativeVueRouteIds` = 已可用菜单）；路由映射 `web/src/vue/router.ts`
- API 客户端：`web/src/clinical-api.ts`；页面样例 `web/src/vue/views/OrganizationAdministrationPage.vue`
- 合成数据种子：`src/main/java/org/openemr2026/development/SyntheticDataImporter.java`
- 证据报告：`docs/process/testing/2026-08-22-*.md`
