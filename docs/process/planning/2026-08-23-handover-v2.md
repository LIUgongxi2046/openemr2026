# openemr2026 交接文档 v2（2026-08-23 全量开发完成后）

> 读者：接续开发 agent。开工先读 `DEVELOPMENT_PRINCIPLES.md` + `docs/process/planning/2026-08-23-handover.md`（v1）+ 本文档。
> 本文档回答一个核心问题：**前后端/数据库/Agent 四层分别开发到什么程度，还差什么。**

## 0. 总览

- **菜单主度量：194/194 可用，0 个 `NOT_AVAILABLE`**（本冲刺从 150 → 194）。
- 技术栈不变：Vue 3 单栈前端 + Spring Boot 模块化单体后端（JdbcClient + 显式 SQL）+ PostgreSQL 单一事实源 + Flyway V1–V162。
- 验证全绿：`npm run build`=0、`vue-tsc`=0、`vitest`=17/17、浏览器路由审计 194/194、契约 check 443 schemas 无漂移、red-team/route-map/security-scan 全 PASS。

**关键诚实结论**：菜单层 100% 打通，但**功能层不是 100%**。194 个路由里约 42 个是「演示/占位」形态（模拟适配器 13 + 可视化配置草稿 13 + 模拟看板 14 + 模拟接口控制台等），它们**没有真实后端业务语义**。真正的功能完整性仍需三类后端支撑：真实适配器、配置持久化引擎、指标语义层。

---

## 1. 前端完成度（194/194 路由可用）

### 1.1 生产纵向（约 152 路由，真实合成 API 端到端）
临床主链（MPI→就诊→病历→诊断/医嘱→执行/结果→质控/签署→病案封存）、门急诊/预约、药房/收费/检验/影像/输血/手术/护理、病案资产/院感/评级/资质、专科中心 + 10 专科七层 71 路由、治理/字典/能力包/迁移、数据质量/科研/开源、模型目录/评估、Agent/Skill/Tool 目录、Prompt release、动作审批、提醒、集成映射、专科覆盖、角色、管理审计（`admin-audit`）、全局临床 AI 助手（`ai-assistant`）、三域卡片门户（`clinical`）。

### 1.2 模拟接口适配器（13 个，调 mock 域，**待真实适配器**）
`admin-auth`、`ai-capture`、`model-connection`、`model-routing`、`devices`、`device-monitoring`、`integration-connectors`、`integration-messages`、`archive-scan`、`archive-preservation`、`pathology-workbench`、`anesthesia-workbench`、`therapy-workbench`。
形态：页头 + 「连接测试」按钮 → 调 `mock` 域 `/mock-interfaces/{code}/invoke` 返回确定性合成数据。**未接任何真实外部系统。**

### 1.3 可视化配置草稿（13 个，**纯前端 ref 数据，无后端**）
`workflow`、`form-designer`、`rule-center`、`scope-designer`、`config-release`、`config-upgrade`、`admin-master-data`、`admin-parameters`、`admin-jobs`、`backup`、`install`、`operations`、`release-gates`。
形态：本地合成配置列表 + 表单，写操作只更新本地 ref，**未持久化、无画布/校验/版本/灰度引擎**。

### 1.4 模拟看板（14 个，**纯前端 ref 数据，无后端**）
`data-center`、`research`、`research-stats`、`department-qc`、`quality-center`、`agent`、`agent-compose`、`agent-context`、`agent-evals`、`ai-center`、`ai-assistant-policy`、`opd-followup`、`unified-home`、`login-context`。
形态：硬编码合成指标 grid + 表格，**无语义层/统计引擎/运行时编排**。

### 1.5 其他前端缺口
- **D4 逐页像素级视觉复核**：代码层对齐已完成（壳层/卡片/表格/指标/徽标/按钮/门户/急诊三栏/住院三栏/页头指标），但「对照原型逐页像素级复核」需设计/生图**人工验收**（`docs/design/ui-delivery/ui-audit-2026-08-23.md`）。

---

## 2. 后端完成度

规模：576 个 Java 文件，135 个 Service + 134 个 Controller。

### 2.1 已实现（真实领域逻辑，dev-synthetic 端到端可验证）
临床主链状态机/四级审签/诊断/医嘱/结果危急值/会诊/临床路径/用药安全硬门/统一任务/护理/交接班/预约号源/病案/输血双人核验/手术三方核查/院感/字典/能力包灰度/数据质量/历史迁移/科研队列/开源指标/专科 71（妇产/生殖/儿科/新生儿/精神/眼/耳鼻喉/口腔/皮肤/中医七层全量）/模型目录/评估/Prompt release/Agent·Skill·Tool 目录/AgentRun（受控运行+预算+消费+依赖解析+输出守卫）/动作审批/提醒/集成映射/专科覆盖/角色/管理审计（`audit` 包）/全局 AI 助手 SSE（`assistant` 包）/模拟接口（`mock` 包）。

### 2.2 未实现（按性质分组）

**A. 真实外部适配器（13 个模拟适配器的真实版）**
- 真实 OIDC/MFA（`admin-auth` 真实 IdP）
- 真实语音转写 ASR（`ai-capture`）
- 真实模型 Provider 连接/路由（`model-connection`/`model-routing`，当前用 `DeterministicFakeClinicalModelProvider`）
- 真实设备网关（`devices`/`device-monitoring`）
- 真实 LIS/PACS/HIS/CA 连接器 + 消息追踪对账（`integration-connectors`/`integration-messages`）
- 真实扫描仪 + 长期保存存储（`archive-scan`/`archive-preservation`）
- 真实病理/麻醉/治疗专科系统（`pathology-workbench`/`anesthesia-workbench`/`therapy-workbench`，X01/H01 未实现）

**B. 配置持久化引擎（13 个可视化配置路由的真实版，G01 重设计）**
- 流程定义/表单模板/规则/职责范围/配置版本/发布/升级/主数据/参数/任务调度 的**后端 CRUD + 版本 + 灰度 + 审批**（当前 13 个路由零后端）。
- 备份/装机/运维/发布门禁 的真实运维底座。

**C. 指标语义层 / 统计引擎（14 个模拟看板的真实版）**
- 数据中心指标语义、科研队列统计、院科质控规则引擎、质量中心（当前零后端）。
- 门诊随访后端（`opd-followup`，缺后端）。
- Agent 运行时编排三件套：`agent-compose`（组合画布后端）、`agent-context`（上下文/记忆策略后端）、`agent-evals`（评估发布中心后端——注意 `model-evaluation` 有后端但 evals 编排层没有）。

**D. 生产真实依赖（prod profile 启动门禁明确 NO-GO）**
真实 OIDC/MFA、CA/时间戳、KMS、对象存储、集成证书、真实模型 Provider —— 均为发布风险项，保持 NO-GO，不伪造。

---

## 3. 数据库完成度

Flyway V1–V162（162 个迁移），全部通过 `test-schema.sh`（迁移 + 断言）。

### 3.1 已建表（真实业务）
患者/就诊/病历文档版本/医嘱/结果/签署/审计事件/Outbox/幂等/上下文租约/号源队列/药房/收费/检验标本/影像/输血/手术/护理/交接班/病案资产/院感/评级/资质/专科 71 各层/字典/能力包/数据质量/迁移/科研队列/开源指标/模型/评估/Prompt/Agent/Skill/Tool/AI Run/预算/消费/提醒/动作审批 等。

### 3.2 未建表（对应上述占位路由，零后端所以零表）
- **配置域**：流程定义、表单模板、规则、职责范围、配置版本/发布、主数据、系统参数、任务调度。
- **集成/设备/适配器域**：连接器目录、集成消息追踪、设备目录、扫描批次、长期保存、病理/麻醉/治疗记录。
- **指标/科研/质控语义层**：数据中心指标、科研统计分析、院科质控、质量中心聚合表。
- **Agent 编排层**：组合画布、上下文/记忆策略、评估发布流水线。
- **门诊随访**：随访/终诊表。

---

## 4. Agent 完成度

### 4.1 已实现（真实后端）
- Agent 目录（`agent-catalog`，V80）+ Skill 目录（V84）+ Tool 目录（V85）+ 依赖解析 + 输出守卫。
- Agent 受控运行（`AgentRunController` + V4 AI run/proposal）+ 运行预算（V132 预算/消费）+ 审批 + 失败恢复。
- Prompt release（V76 版本化/唯一 ACTIVE）。
- 模型目录/评估（V52/V87）。
- 确定性假模型 Provider（`DeterministicFakeClinicalModelProvider`，dev-synthetic）。
- 全局临床 AI 助手（`assistant` 包 SSE，本轮新增）。

### 4.2 未实现（前端为模拟看板）
- `agent`（受控运行 UI）——**有后端**（AgentRun），前端是模拟看板占位，未接真实运行快照/事件流。
- `agent-compose`（组合画布）——**无后端**，需组合画布 + 依赖锁定 + 权限交集校验。
- `agent-context`（上下文/记忆策略）——**无后端**，需患者/就诊隔离 + 最小字段 + 过期/撤权。
- `agent-evals`（评估发布中心）——**编排层无后端**（`model-evaluation` 有，但 evals 的供应链扫描/红队/影子/灰度发布没有）。
- 真实模型 Provider 联调（当前确定性假模型）。

---

## 5. 验证状态与已知问题

| 项 | 状态 |
|---|---|
| `npm run build`（check:no-react + vue-tsc + vite） | ✅ 0 |
| `npx vitest run` | ✅ 17/17 |
| 浏览器路由审计（194 路由 H1/导航/溢出/控制台/HTTP/未知深链） | ✅ 194/194 |
| 契约 check（443 schemas 无漂移）+ 契约测试 | ✅ |
| red-team / route-map / security-scan | ✅ PASS |
| 后端 Java 编译 | ✅ 0 |
| 新增测试（AuditEventApiTest / MockInterfaceApiTest） | ✅ 通过 |
| **`OutboxDispatcherTest`（预存，非本轮引入）** | ❌ 2 个异步投递测试失败（期望 PUBLISHED/DEAD_LETTER 实得 PENDING），Outbox 异步时序问题，建议单独排查 |

---

## 6. 建议下一步（按优先级）

1. **真实适配器接入**（13 个模拟适配器）：优先 LIS/PACS/HIS/CA + IdP + 设备网关，替换 `mock` 域 handler。
2. **G01 可视化配置重设计**（13 个）：先做 `workflow`（流程画布，原型有 demo）+ `form-designer`/`rule-center`/`scope-designer`，后端 CRUD + 版本 + 灰度。
3. **指标语义层/统计引擎**（14 个看板）：先 `data-center`/`research-stats`/`quality-center`。
4. **Agent 运行时编排**（agent-compose/context/evals）：A01 顶层，接真实运行快照/事件流。
5. **门诊随访后端**（`opd-followup`）。
6. **Outbox 异步测试排查**（预存 2 失败）。
7. **D4 逐页像素级人工验收**（设计/生图）。

> 一句话给接续者：**菜单已 100% 打通，但 40 个路由是「演示形态」；真正未完成的是三大后端引擎（真实适配器 / 配置持久化 / 指标语义）+ Agent 编排层 + 门诊随访后端。**
