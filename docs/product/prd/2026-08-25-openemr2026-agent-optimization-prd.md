# openemr2026 Agent 优化版 PRD（融合 Agent / Skill / Tool）

> - 文档版本：v0.2
> - 日期：2026-08-25（Asia/Shanghai）
> - 文档状态：`CREATED`，待产品、临床、架构、安全与测试联合评审
> - 需求模式：`UPDATE`
> - 适用版本：openemr2026 v1.x 增量能力
> - 上位需求：[`2026-08-11-openemr2026-v1.0-prd.md`](2026-08-11-openemr2026-v1.0-prd.md)
> - 调研依据：[`2026-08-25-medical-agent-skill-tool-landscape.md`](../research/2026-08-25-medical-agent-skill-tool-landscape.md)
> - 技术承接：S003 原型、S004 HLD、S005 Agent/数据/后端 LLD、S009 测试与 AI Evals

## 0. 文档控制

### 0.1 目的

本 PRD 将主 PRD 中的平台级 AI 能力，收敛为可直接进入原型、架构、研发和验收的 Agent 优化版本。重点不是增加一个万能聊天框，而是把医疗工作拆成职责窄、可引用、可评测、可停用的 Agent；把可复用医疗流程固化为 Skill；把查询、校验和候选动作固化为最小权限 Tool。

本文定义产品行为、角色责任、状态、异常和验收口径，不替代 Agent LLD、API Schema、数据模型或部署设计。

### 0.2 编号规则

- 本文在文档内使用 `FR-###`、`BR-###`、`AC-###`、`NFR-###`、`RISK-###`、`Q-###`。
- 为避免与主 PRD 的既有编号冲突，全局引用采用 `AOPT-v0.2/FR-001` 形式。
- 主 PRD 映射在第 14 章单独维护；本文不重编号、不覆盖主 PRD。

### 0.3 修订记录

| 版本 | 日期 | 状态 | 说明 |
|---|---|---|---|
| v0.1 | 2026-08-25 | `CREATED` | 首版：融合市场调研、主 PRD、Agent LLD、当前 6 个前台 Agent 与现有审批/执行治理切片 |
| v0.2 | 2026-08-25 | `CREATED` | 根据产品反馈，将 P0 能力明确为主 Agent，新增“统一入口—主 Agent—诊疗环节子 Agent—Skill—Tool”层级、33 个 P0 候选子 Agent、诊疗全流程路由及父子运行验收 |

## 1. 输入事实、推断与缺口

| ID | 类型 | 内容 | 证据/处理 |
|---|---|---|---|
| FACT-001 | `EXPLICIT` | AI 不得自主诊断、处方、签署或执行临床动作。 | 主 PRD BR-025/026/060；作为硬边界。 |
| FACT-002 | `EXPLICIT` | AI 输出必须与临床事实分层，并保留来源、版本和人工处理结果。 | 主 PRD BR-028/030/040。 |
| FACT-003 | `OBSERVED` | 项目已有 Agent、Skill、Tool 注册表和依赖表。 | `V80/V84/V85/V94` 数据库迁移；仅证明目录治理基础存在。 |
| FACT-004 | `OBSERVED` | 当前前台目录有 `OPD_COPILOT`、`DIAGNOSIS_REVIEW`、`ORDER_SAFETY`、`RESULT_TRIAGE`、`DOCUMENT_QC`、`CARE_COORDINATOR`。 | `web/src/vue/agent-assistant-catalog.ts`。 |
| FACT-005 | `OBSERVED` | 本地已验证动作审批与执行状态治理、职责分离、幂等和审计。 | A02 测试报告；不等同真实下游副作用已接通。 |
| FACT-006 | `OBSERVED` | Agent LLD 已定义 Context Lease、Tool Gateway、AIProposal、Verification Agent 和受控运行状态机。 | `docs/design/architecture/2026-08-14-openemr2026-lld-agent.md`。 |
| FACT-007 | `OBSERVED` | 成熟医疗 AI 主要集中在摘要、文书、质控、消息草稿、编码与运营辅助，而非自主医生。 | 专题调研，证据截止 2026-08-25。 |
| FACT-008 | `INFERRED` | openemr2026 的差异化资产应主要沉淀在 Skill 契约、Tool 契约、评测集和治理证据，而不是绑定单一模型。 | 来自产品架构与市场证据的综合判断；需评审。 |
| FACT-009 | `INFERRED` | 当前 6 个前台 Agent 应作为用户心智入口或主 Agent 入口，运行时按诊疗环节映射到子 Agent、Skill 与 Tool。 | 降低迁移成本并避免大而全 Agent；需原型验证。 |
| FACT-010 | `UNKNOWN` | 目标医院、首批科室、中文模型/部署、知识源、数据驻留和模型预算尚未批准。 | 不在本 PRD 中虚构；列入 Q-001–005。 |
| FACT-011 | `UNKNOWN` | 各场景真实基线时间、采用率、采纳后修改率和告警负担尚无本地数据。 | 先埋点和影子实验，再设收益阈值。 |
| FACT-012 | `EXPLICIT` | v0.1 中的 P0 Agent 应视为主 Agent，并继续按不同诊疗环节拆分子 Agent。 | 2026-08-25 产品反馈；作为 v0.2 的主要变更输入。 |

## 2. 执行摘要

### 2.1 产品问题

当前产品虽然具备 AI 助手入口、Agent/Skill/Tool 名录和治理骨架，但用户面对的仍是功能名称，缺少稳定的任务契约：谁在什么上下文触发哪个 Agent、Agent 可调用哪些 Skill/Tool、结果如何引用、何时必须停下、如何确认或拒绝、失败怎样回到人工流程。若直接扩充通用 Agent，容易出现边界重叠、权限不可解释、输出不可验收和“目录已注册但能力不可运行”的问题。

### 2.2 产品命题

以“一个前台医助入口 + 多个主 Agent + 诊疗环节子 Agent + 版本化 Skill + 最小权限 Tool + 独立验证与人工确认”为核心形态：

1. 前台统一承接意图和上下文，但不拥有无限能力。
2. 主 Agent 对一个端到端用户任务负责，并根据当前诊疗阶段编排白名单子 Agent。
3. 子 Agent 对一个可独立失败、恢复和评测的诊疗环节负责，不直接扩展主 Agent 权限。
4. Skill 固化医疗工作流、模板、规则、Schema、失败语义和 Evals；没有独立目标和运行状态的能力不得仅为“显得智能”而注册成子 Agent。
5. Tool 只提供确定性、强 Schema、可审计的能力；任何副作用均进入候选或原业务审批状态机。
6. 临床用户看到来源、不确定项、变化差异和明确动作，再决定接受、修改、拒绝或接管。

### 2.3 目标

| ID | 目标 | 成功判据 |
|---|---|---|
| G-001 | 形成可落地的首批医疗 Agent 组合 | 5 个 P0 主 Agent 和 33 个候选诊疗环节子 Agent 均有角色、触发、输入、输出、边界、Skill/Tool 依赖、状态与 AC，并在 R0 决定哪些子 Agent 应下沉为 Skill。 |
| G-002 | 让 AI 结果可复核而非只“看起来合理” | 临床事实性陈述可回到来源；无证据项明确标识。 |
| G-003 | 让 Agent 权限和副作用可控 | 未授权 Tool 调用、跨患者引用和未批准副作用均被阻止并审计。 |
| G-004 | 让能力可持续演进 | Agent/Skill/Tool 版本锁定、评估、灰度、回滚和紧急停用形成闭环。 |
| G-005 | 证明真实工作流价值 | 通过影子和小规模灰度获得净复核时间、质量、采用和安全基线。 |

### 2.4 非目标

- 不建设自主诊断、自动处方、自动签署、自动给药/治疗或自动解除临床安全阻断。
- 不把模型自述、通用基准或演示成功当作临床有效性证明。
- 不允许 Agent 使用通用 SQL、通用 HTTP、任意代码执行或无患者范围的搜索 Tool。
- 不在首版建设无界多 Agent 社会、长期跨患者记忆或自我创建 Skill/Tool。
- 不在首版向患者开放急症症状分诊或未经批准的个体化治疗建议。
- 不在本 PRD 中承诺具体节时、采纳率或诊疗质量提升数值；这些目标须基于本地基线批准。

## 3. 用户、角色与责任

| 角色 | 核心任务 | 可做 | 不可做 |
|---|---|---|---|
| ROLE-001 临床医生 | 快速理解患者、草拟文书、闭环结果和协同 | 发起授权 Agent；查看来源；编辑/接受/拒绝候选；按原流程签署或批准 | 将 AI 一键视为已确认事实；借 Agent 越权查看患者 |
| ROLE-002 护士 | 交班、任务与异常结果协同 | 使用护理范围 Skill；审阅交班/任务候选 | 将计划或建议标记成已执行护理事实 |
| ROLE-003 质控/病案/编码人员 | 发现缺陷、复核编码与整改 | 查看授权版本；处理质控/编码候选；反馈误报漏报 | 修改已签原文；把 AI 建议升级为法规硬规则 |
| ROLE-004 会诊/随访协调员 | 准备会诊、转诊、交班、随访任务 | 审阅摘要和任务候选；按授权提交 | 代替责任医生确认临床结论 |
| ROLE-005 AI 治理管理员 | 配置用例、发布、观测和停用 | 管理 Agent/Skill/Tool 候选、路由、门禁、灰度、回滚 | 单独批准自己创建的高风险版本；查看无业务必要的病历正文 |
| ROLE-006 Agent/Skill 设计者 | 沉淀可复用能力包 | 定义契约、依赖、样例和评测集 | 因创作身份获得真实患者数据或生产 Tool 权限 |
| ROLE-007 临床能力负责人 | 判断场景适用性和临床风险 | 批准用例边界、样例、严重错误定义和灰度 | 替代信息安全/隐私审批 |
| ROLE-008 安全/隐私负责人 | 审查数据边界、权限和供应链 | 批准数据分类、外部连接、红队和例外 | 独立宣告临床质量合格 |
| ROLE-009 运维/支持 | 运行观测、事件处置和恢复 | 查看脱敏轨迹；隔离组件；执行批准回滚 | 查看模型秘密或无关患者正文；修改临床结果 |

有效权限始终是“调用用户 × Agent × Skill × Tool × 机构策略 × 当前业务状态”的交集，不因作者、服务账户或模型能力扩大。

## 4. 版本范围与优先级

### 4.1 P0：首个可验证版本

| 主 Agent | 产品目标 | 自治上限 | 核心 Skill | 核心 Tool 类型 |
|---|---|---:|---|---|
| `ENCOUNTER_SUMMARIZER` | 诊前、分诊、当次、住院阶段和出院准备带来源摘要 | A1 | `encounter-previsit-summary@1`、`inpatient-course-summary@1` | 患者/就诊、文书、结果、医嘱、任务只读 |
| `DOCUMENT_DRAFTER` | 生成门诊、病程、出院、交班文书候选 | A1 | `outpatient-note-draft@1`、`discharge-summary-draft@1`、`nursing-shift-handoff@1` | 只读上下文 + `create_document_proposal` |
| `RECORD_QC` | 在硬规则之后发现语义遗漏、冲突和时序问题 | A1 | `record-semantic-qc@1` | 不可变文书、硬规则结果、知识检索、质控候选 |
| `RESULT_FOLLOWUP_COORDINATOR` | 聚合异常结果和未闭环责任 | A1；任务 A2 | `result-trend-and-followup@1` | 结果/任务只读 + `create_task_proposal` |
| `CARE_COORDINATOR` | 准备会诊、转诊、交班与随访摘要/任务 | A1；任务 A2 | `consult-referral-brief@1` | 上下文只读 + 任务/预约变更候选 |

P0 同时包含：统一入口和上下文条、运行进度与接管、来源审阅、候选差异、Agent/Skill/Tool 能力卡、评估发布、审批、观测、反馈、紧急停用和人工降级。

### 4.2 P1：完成 P0 临床灰度后

- `SAFETY_REVIEWER`：只解释确定性用药/医嘱规则和证据，不替代规则引擎。
- `CODING_ASSISTANT`：基于已签文书和有效术语版本生成编码候选。
- `PATIENT_MESSAGE_DRAFTER`：仅白名单低风险类别，红旗症状转人工。
- `AMBIENT_DOCUMENTATION`：完成显式同意、录音状态、说话人/时间引用和音频保留策略后进入灰度。
- `RESEARCH_MATCHER`：仅研究授权场景，输出条件级满足/不满足/未知，不自动入组。

### 4.3 P2 / 暂缓

- 鉴别诊断/治疗方案第二视角仅限研究或影子评估。
- 获批影像、病理、心电 AI 作为外部 Tool 接入，不在本项目重造诊断模型。
- 低风险行政自动化可评估 A3；临床动作长期以 A1/A2 为上限。

### 4.4 Agent 五层产品结构

| 层级 | 对象 | 责任 | 用户可见性 | 不承担什么 |
|---|---|---|---|---|
| L0 | `AI_MEDICAL_ASSISTANT` 统一入口 | 维护当前上下文、识别意图、选择用例和主 Agent、呈现统一审阅体验 | 始终可见 | 不产生临床结论，不直接调用临床副作用 Tool |
| L1 | 主 Agent | 对一个端到端用户任务负责；选择环节子 Agent、汇总结果、处理部分失败并交付一个最终候选 | 显示主 Agent 名称、目标和状态 | 不自行扩展场景，不把所有诊疗环节塞入一个 Prompt |
| L2 | 子 Agent | 对一个诊疗环节或独立工作单元负责；有明确输入输出、预算、状态、失败和独立 Eval | 默认在“运行详情”可见；高风险/失败时前台显著显示 | 不越过主 Agent 目标，不直接成为新的万能入口 |
| L3 | Skill | 封装模板、知识、规则编排、Schema、失败语义和评测样例 | 能力详情可见，普通用户无需手工选择 | 不拥有独立临床身份；单步转换不注册为子 Agent |
| L4 | Tool | 完成确定性查询、校验、计算、检索、保存候选或受控业务动作 | 只读调用默认折叠；副作用必须预览 | 不自主规划，不绕过领域 API、权限和状态机 |

L0–L4 之下是患者、就诊、文书、诊断、医嘱、执行、结果、任务、签署和审计等权威临床内核。任何层级的 AI 输出都不能替代这些对象的业务终态。

### 4.5 P0 主 Agent 与诊疗环节子 Agent 目录

#### 4.5.1 `ENCOUNTER_SUMMARIZER` 就诊摘要主 Agent

| 子 Agent | 适配环节 | 触发/输入范围 | 结构化输出 | 硬边界 |
|---|---|---|---|---|
| `PRE_VISIT_SUMMARIZER` | 预约后、到诊前、复诊准备 | 既往就诊、问题、过敏、当前用药、近期结果、开放任务 | 诊前摘要、近期变化、待核问题 | 不生成本次诊断或治疗计划 |
| `TRIAGE_CONTEXT_SUMMARIZER` | 门诊分诊、急诊预检 | 主诉、生命体征、既往风险、分诊事实 | 分诊上下文摘要、缺失资料、风险来源 | 分诊级别和急救触发由确定性规则/人工决定 |
| `ACTIVE_ENCOUNTER_SUMMARIZER` | 接诊中、问诊/查体后 | 当次录入、历史摘要、新结果、诊疗任务 | 当次事实摘要、新旧变化、待确认问题 | 不把未确认陈述写成事实 |
| `INPATIENT_DAILY_SUMMARIZER` | 住院每日查房前 | 上次病程后的医嘱、执行、结果、体征、护理、会诊和任务 | 24 小时/指定时窗事件摘要、趋势和未闭环项 | 不声称医嘱已执行或任务已完成 |
| `PERIOPERATIVE_CONTEXT_SUMMARIZER` | 术前准备、术后交接 | 手术申请、术前检查、风险、知情、执行事件和术后计划 | 围术期事实包、缺项和来源 | 患者/部位/侧别/核查状态以硬规则和业务状态为准 |
| `DISCHARGE_READINESS_SUMMARIZER` | 出院计划与出院前对账 | 医嘱、结果、会诊、文书、费用、用药、随访和未决任务 | 出院准备摘要、未闭环清单和责任角色 | 不决定是否准予出院，不伪造豁免 |

#### 4.5.2 `DOCUMENT_DRAFTER` 文书主 Agent

| 子 Agent | 适配环节 | 主要输出 | 主要 Skill/来源 | 硬边界 |
|---|---|---|---|---|
| `OUTPATIENT_NOTE_DRAFTER` | 门诊接诊 | 门诊病历候选 | 门诊模板、当次录入、已确认事实 | 不自动确认诊断或签署 |
| `EMERGENCY_NOTE_DRAFTER` | 急诊分诊、抢救、留观 | 急诊记录候选和时间关键事件缺口 | 急诊模板、事件时间线、医嘱/执行/结果 | 不推测未记录的抢救动作 |
| `ADMISSION_NOTE_DRAFTER` | 入院 24 小时内 | 入院记录候选 | 入院模板、病史、查体、既往资料 | 缺失查体不得自动补“正常” |
| `FIRST_COURSE_DRAFTER` | 入院后首次病程 | 首次病程候选 | 入院资料、诊断依据、鉴别要点、计划 | 诊断和计划保持候选状态 |
| `PROGRESS_NOTE_DRAFTER` | 日常病程 | 阶段/日常病程候选 | 上次病程后事件、结果、医嘱和效果 | 不把计划写成已执行事实 |
| `WARD_ROUND_NOTE_DRAFTER` | 主治/主任查房 | 查房记录候选、待补上级意见 | 查房事实、上级意见、病情变化 | 不能代替实际查房者或上级签名 |
| `CONSULT_NOTE_DRAFTER` | 会诊申请/意见 | 会诊申请或意见候选 | 会诊目标、授权材料、实际会诊输入 | 申请方不能代写会诊方最终意见 |
| `PERIOPERATIVE_NOTE_DRAFTER` | 术前、手术、术后 | 术前小结/讨论、手术/术后记录候选 | 手术业务对象、事件、设备/人员来源 | 不臆造手术、植入物或麻醉事实 |
| `NURSING_HANDOFF_DRAFTER` | 护理班末/转单元 | 护理交班候选 | 体征、给药/治疗执行、护理计划、风险和任务 | 不把待执行计划写成已完成 |
| `DISCHARGE_NOTE_DRAFTER` | 出院/死亡终末文书 | 出院记录、小结或终末记录候选 | 入院原因、医院经过、诊断、用药、未决事项 | 未闭环事项必须显式列出；不自动签署 |

#### 4.5.3 `RECORD_QC` 病历质控主 Agent

| 子 Agent | 适配环节 | 质控范围 | 输出 | 硬边界 |
|---|---|---|---|---|
| `WRITING_QC_REVIEWER` | 书写中 | 当前草稿结构、来源、前后文 | 实时低打扰建议 | 不执行法规/机构硬阻断 |
| `PRE_SIGN_QC_REVIEWER` | 签署前 | 完整性、一致性、时序、必要来源和签署条件 | 待修改项、来源和严重度建议 | 硬规则结果单独保留且优先 |
| `ACTIVE_RECORD_QC_REVIEWER` | 运行病历抽查 | 在院/在诊文书版本、逾期、复制与诊疗一致性 | 缺陷候选和责任角色 | 不直接创建正式缺陷终态或改原文 |
| `TERMINAL_RECORD_QC_REVIEWER` | 出院/归档前 | 文书、首页、编码、结果、签名和整改闭环 | 终末质控候选和缺口清单 | 归档阻断/豁免由确定性规则和授权人决定 |
| `CORRECTION_CONSISTENCY_REVIEWER` | 更正、补充、来源报告更正后 | 新旧版本、引用来源、受影响文书 | 差异和需复核对象清单 | 不静默改写已签文书 |

质控维度中的“完整性、语义一致性、时序、复制前移、来源引用、专科约束”等是可复用 Skill；它们没有独立临床任务状态时，不单独注册成子 Agent。

#### 4.5.4 `RESULT_FOLLOWUP_COORDINATOR` 结果闭环主 Agent

| 子 Agent | 适配环节 | 触发/输入范围 | 输出 | 硬边界 |
|---|---|---|---|---|
| `NEW_RESULT_INTAKE_AGENT` | 新结果确认/回传 | 新增或更正结果、原申请、患者/就诊 | 新结果摘要、优先级依据和相关任务 | 结果确认状态以 LIS/PACS/领域系统为准 |
| `RESULT_TREND_REVIEWER` | 结果查看、查房、复诊 | 同项目/相关项目的版本化结果和单位 | 趋势、异常变化、不可比较项 | 单位/参考范围转换使用确定性 Tool |
| `CRITICAL_RESULT_CONTEXT_AGENT` | 危急值事件 | 危急值规则结果、通知/接收/处置状态 | 风险上下文和未完成状态 | 不判断危急值、不自动标记已通知或已处置 |
| `PENDING_RESULT_TRACKER` | 就诊结束、出院和随访 | 已申请未报告、部分报告、更正中结果 | 待回结果清单、责任和时限 | 不把未报告解释为阴性 |
| `FOLLOWUP_TASK_PLANNER` | 异常结果需复查/随访 | 已确认结果、已确认计划、责任策略 | 任务候选、对象、时限和证据 | 不自行决定治疗方案或发送患者消息 |
| `CORRECTED_RESULT_RECONCILER` | 报告更正/撤回 | 原结果、新版本、引用文书和已建任务 | 受影响对象、复核和撤销/更正候选 | 不修改已签文书或删除历史任务 |

#### 4.5.5 `CARE_COORDINATOR` 诊疗协同主 Agent

| 子 Agent | 适配环节 | 输入 | 输出 | 硬边界 |
|---|---|---|---|---|
| `CONSULT_PREPARATION_AGENT` | 会诊前 | 会诊目标、病史、结果、已处置事项 | 会诊摘要、问题清单和资料缺口 | 不代替会诊医师出结论 |
| `MDT_BRIEF_AGENT` | MDT 讨论前 | 多学科已授权资料和议题 | 结构化病例简报、分歧和待决问题 | 不伪造会议共识 |
| `TRANSFER_HANDOFF_AGENT` | 转床、转科、转院 | 当前问题、医嘱、执行、风险和未完任务 | 交接候选、接收方和未完事项 | 实际交接与接收状态来自业务流程 |
| `DISCHARGE_TRANSITION_AGENT` | 出院准备 | 出院诊断/用药/教育/复诊和未决结果 | 连续照护计划候选和风险清单 | 只改写已确认计划，不新增治疗建议 |
| `FOLLOWUP_COORDINATION_AGENT` | 院后随访 | 已确认随访计划、结果和任务策略 | 随访任务/提醒候选和人工升级条件 | 症状分诊和高风险消息转人工 |
| `TASK_RECONCILIATION_AGENT` | 交班、转科、出院或恢复后 | 开放、重复、过期、转派中的任务 | 去重/转派/关闭建议和依据 | 通知已读不等于任务完成；终态由来源系统确认 |

### 4.6 按诊疗环节的主 Agent 路由

| 诊疗环节 | 默认主 Agent | 主要子 Agent | 人工责任点 | 不可由 Agent 完成 |
|---|---|---|---|---|
| 预约后/诊前准备 | EncounterSummarizer | PreVisitSummarizer | 医生确认需关注问题 | 新诊断、用药调整 |
| 门诊分诊/急诊预检 | EncounterSummarizer | TriageContextSummarizer | 分诊人员确认级别和升级 | 分诊定级、急救决策 |
| 接诊问诊/查体 | EncounterSummarizer + DocumentDrafter | ActiveEncounterSummarizer、Outpatient/EmergencyNoteDrafter | 医生确认事实与文书 | 自动诊断、签署 |
| 诊断形成与复核 | P1 DiagnosisReview | EvidenceGap、Terminology、PlanConsistency 子 Agent | 医生形成和确认诊断 | 确诊、替代鉴别判断 |
| 医嘱/处方准备 | P1 SafetyReviewer | MedicationReconciliation、Allergy、Dose、Interaction 子 Agent | 医生/药师处理规则和批准医嘱 | 开方、生效、解除硬阻断 |
| 执行与床旁观察 | EncounterSummarizer + CareCoordinator | InpatientDailySummarizer、TaskReconciliationAgent | 护士/执行者记录真实执行 | 给药、治疗、输血、手术执行 |
| 新结果/危急值 | ResultFollowupCoordinator | NewResultIntake、CriticalResultContext、TrendReviewer | 医务人员确认、通知和处置 | 危急值判定、自动声称已处置 |
| 住院查房/连续病程 | EncounterSummarizer + DocumentDrafter | InpatientDailySummarizer、Progress/WardRoundDrafter | 本人/上级审阅签署 | 代替实际查房和上级意见 |
| 会诊/MDT/转科 | CareCoordinator + DocumentDrafter | ConsultPreparation、MDTBrief、TransferHandoff、ConsultNoteDrafter | 发起方/接收方分别确认 | 伪造会诊结论或交接完成 |
| 围手术期 | EncounterSummarizer + DocumentDrafter | PerioperativeContextSummarizer、PerioperativeNoteDrafter | 手术团队完成核查和记录 | 核查、手术、麻醉或植入物事实 |
| 出院准备/出院 | EncounterSummarizer + CareCoordinator + DocumentDrafter | DischargeReadiness、DischargeTransition、DischargeNoteDrafter | 责任医生决定出院并签署 | 批准出院、豁免缺项 |
| 随访与结果追回 | ResultFollowupCoordinator + CareCoordinator | PendingResultTracker、FollowupTaskPlanner、FollowupCoordination | 责任团队确认计划和任务 | 高风险症状自动分诊/处置 |
| 书写中/签署前质控 | RecordQC | WritingQC、PreSignQC | 作者整改、签署人确认 | 降低硬规则严重度、自动改原文 |
| 运行病历/终末/归档 | RecordQC | ActiveRecordQC、TerminalRecordQC、CorrectionConsistency | 质控/病案人员复核和关闭缺陷 | 自动豁免、归档或覆盖签署版本 |

同一页面可以组合多个主 Agent，但一次用户任务只能有一个主 Agent 对最终结果负责；其他主 Agent 以受控依赖或新任务运行，避免互相修改对方候选。

### 4.7 P1 主 Agent 的预规划子 Agent

| 主 Agent | 候选子 Agent | 定位与限制 |
|---|---|---|
| `DIAGNOSIS_REVIEW` | `EVIDENCE_GAP_REVIEWER`、`DIFFERENTIAL_GAP_REVIEWER`、`DIAGNOSIS_TERMINOLOGY_MAPPER`、`DIAGNOSIS_PLAN_CONSISTENCY_REVIEWER` | 默认 `EXPERIMENTAL`；只检查依据、缺口、术语和一致性，不确认诊断或推荐治疗终态 |
| `SAFETY_REVIEWER` | `MEDICATION_RECONCILIATION_AGENT`、`ALLERGY_CONTRAINDICATION_REVIEWER`、`DOSE_RULE_EXPLAINER`、`INTERACTION_DUPLICATION_REVIEWER`、`EXECUTION_PREREQUISITE_REVIEWER` | 规则、剂量和相互作用结论优先来自确定性 Tool；Agent 负责汇总与解释 |
| `CODING_ASSISTANT` | `DOCUMENT_EVIDENCE_REVIEWER`、`ICD_CANDIDATE_AGENT`、`PROCEDURE_CODE_CANDIDATE_AGENT`、`CDI_QUERY_DRAFTER` | 仅基于已签/授权文书和有效术语版本；编码员或医生确认 |
| `PATIENT_MESSAGE_DRAFTER` | `MESSAGE_CLASSIFIER`、`RED_FLAG_ESCALATION_AGENT`、`RESPONSE_DRAFTER`、`READABILITY_REVIEWER` | 只处理白名单消息；红旗症状和证据不足转人工，不自动发送 |
| `AMBIENT_DOCUMENTATION` | `CONSENT_SESSION_GUARD`、`TRANSCRIPT_SEGMENT_AGENT`、`SPEAKER_ATTRIBUTION_REVIEWER`、`NOTE_ASSEMBLY_AGENT` | ASR 是 Tool；子 Agent 管会话、片段、说话人和文书装配，录音必须显式开始/停止 |
| `RESEARCH_MATCHER` | `PROTOCOL_CRITERIA_PARSER`、`PATIENT_CRITERIA_MATCHER`、`MISSING_DATA_REVIEWER`、`RESEARCH_BRIEF_AGENT` | 仅研究授权数据；输出条件级满足/不满足/未知，不自动入组 |

## 5. 产品对象与信息架构

| 对象 | 用户可理解的定义 | 必须展示/记录的关键字段 |
|---|---|---|
| AgentDefinition | 为一个明确任务负责的受控执行者 | 名称、`MAIN/SUB` 层级、父主 Agent、适用诊疗环节、目标、角色/场景、自治等级、上下文、Skills、Tools、预算、停止/接管、输出、版本、状态、负责人 |
| AgentComposition | 主 Agent 可编排的子 Agent 有向无环组合 | 主 Agent 版本、子 Agent 白名单/版本、触发条件、输入输出映射、顺序/并行、总预算、部分失败策略、人工接管和最终聚合契约 |
| SkillDefinition | 可复用且可评测的医疗工作流包 | 用途、输入/输出 Schema、前后置条件、模板/知识、允许 Tools、失败语义、依赖、Evals、版本、许可证 |
| ToolDefinition | 单一职责的确定性能力 | 只读/候选/副作用等级、输入输出、患者范围、权限、幂等、超时、错误、补偿、审批策略、版本 |
| UseCasePolicy | 某机构/科室/角色对某能力的准入规则 | 场景、数据分类、模型路由、Agent/Skill/Tool 版本、自治上限、阈值、静默/停用、有效期 |
| ContextLease | 一次运行可读取的最小上下文租约 | 主体、机构、患者、就诊、任务、字段范围、来源、版本/水位、过期时间、撤销原因 |
| AgentRun | 一次可追踪、可取消、可恢复的主/子 Agent 运行 | run_id、parent/root run、agent level、调用者、上下文、制品版本、步骤、预算、状态、错误、审批、来源、结果、时间 |
| AIProposal | 与临床事实分离的候选结果 | 类型、患者/就诊、来源、生成版本、差异、不确定项、状态、审阅人、接受/拒绝/过期原因 |
| ActionApproval | 对屏幕所示具体副作用的逐次批准 | 动作、对象、参数、证据、风险、批准人、有效期、payload hash、状态 |
| ActionExecution | 批准后由领域内核执行的结果 | approval_id、幂等键、执行身份、业务结果引用、状态、失败/补偿、核验时间 |
| EvalEvidence | 能力上线与继续运行的证据 | 数据集版本、场景、错误分级、指标、样本量、审阅者、结果、门禁、适用范围、有效期 |

前台信息架构保留“AI 医助小南”统一入口；完整工作区分为“当前任务、诊疗阶段/子任务、来源、候选、运行记录”五区。临床用户默认看到主 Agent 和当前环节；可展开查看子 Agent 的输入范围、状态、结果和失败，但不必理解底层 Skill/Tool 编排。Agent/Skill/Tool 管理进入 AI 治理中心。

## 6. 核心流程与状态

### 6.1 FLOW-01 临床用户发起与审阅

1. 用户从患者页面、任务页、文书页或全局入口发起任务。
2. 系统展示并校验机构、角色、患者、就诊、任务和页面上下文；无患者时进入安全空上下文。
3. 路由到一个已批准主 Agent，固定主 Agent、AgentComposition 和 ContextLease 版本。
4. 主 Agent 根据诊疗环节选择白名单子 Agent；每个子 Agent 获得更小的上下文、预算和允许 Skill/Tool，并形成父子运行轨迹。
5. 主 Agent 汇总子 Agent 的完整、部分或失败结果；用户可查看阶段进度、取消整个任务或接管具体环节。
6. Verification 步骤检查父子 Schema、来源、患者一致性、冲突、预算和政策。
7. 最终结果以一个主 AIProposal 展示来源、不确定项、子任务状态和与当前内容的差异。
8. 用户接受、修改后接受、拒绝或返回人工流程；进入签署/执行的仍走原领域状态机。

### 6.2 FLOW-02 候选动作审批与执行

1. Agent 只能生成动作预案，不能直接产生临床副作用。
2. 审批页展示患者/就诊、对象、关键参数、证据、风险、后果、有效期和差异。
3. 审批人重新鉴权并批准或拒绝；提案者不得自批高风险动作。
4. 执行端重新鉴权、校验上下文和 payload hash，通过领域 API 幂等执行。
5. Agent 读取业务结果进行核验；不能核验则显示“执行状态未知”，进入人工对账。

### 6.3 FLOW-03 能力设计与发布

候选定义 → 契约校验 → 合成数据测试 → 权限/跨患者/提示注入/工具故障红队 → 临床评审 → 安全评审 → 影子 → 灰度 → 批准 → 持续监测。任一硬门禁失败均不得晋升。

### 6.4 FLOW-04 故障、取消与恢复

- 模型/Tool 超时：展示具体失败阶段；只读步骤可按策略重试，副作用不得盲重试。
- 用户取消：停止后续步骤，保留已发生的可审计事实；未完成候选不进入病历。
- 上下文变化：立即过期旧 ContextLease、审批和候选；重新发起后不得复用旧批准。
- 恢复：固定原制品版本，重新鉴权和读取权威事实；已成功副作用按幂等键跳过并核验。
- AI 全部不可用：原有人工登记、文书、医嘱、执行、签署和任务流程仍可完成。

### 6.5 FLOW-05 主 Agent 编排子 Agent

1. 主 Agent 读取当前诊疗环节、页面对象、用户目标和已批准 AgentComposition。
2. 只选择该主 Agent 版本白名单内的子 Agent；生成可解释的阶段计划和总预算。
3. 为每个子 Agent 派生不大于父级的 ContextLease、权限、时间/成本/Tool 预算和幂等范围。
4. 子 Agent 独立运行并返回结构化状态：`COMPLETE`、`INCOMPLETE`、`NO_EVIDENCE`、`BLOCKED` 或 `FAILED`。
5. 主 Agent 按组合契约决定继续、跳过非关键子任务、等待人工补充、取消后续步骤或整体失败。
6. 主 Agent 只能在所有关键子任务和最终 Verification 通过后生成主 AIProposal。
7. 取消父运行必须传播到所有未完成子运行；恢复时逐个核验原子运行和副作用，不得重跑已成功副作用。

### 6.6 状态机

**AgentRun：** `CREATED → ROUTING → RETRIEVING → PLANNING → GENERATING → VERIFYING → READY_FOR_REVIEW → COMPLETED`。

旁路状态：

- 需逐次审批：`PLANNING → WAITING_APPROVAL → GENERATING`。
- 用户接管/拒绝/过期：任一可取消状态 → `CANCELLED`；`WAITING_APPROVAL → CANCELLED`。
- 验证失败：`VERIFYING → BLOCKED`；修复上下文后创建新运行，不原地篡改结果。
- 系统失败：运行态 → `FAILED`；仅符合幂等和恢复策略时进入 `RECONCILING` 后继续。

**子 AgentRun：** `CREATED → QUEUED → RUNNING → COMPLETE | INCOMPLETE | NO_EVIDENCE | BLOCKED | FAILED | CANCELLED`。子 Agent 的 `COMPLETE` 仅表示其结构化子任务完成，不代表主任务完成或任何临床事实生效。

**父子聚合：** 关键子 Agent `BLOCKED/FAILED` 时，父运行不得进入 `READY_FOR_REVIEW`；非关键子 Agent 失败仅在组合契约明确允许时进入 `PARTIAL` 聚合，并在用户界面显著列出缺失范围。

**AIProposal：** `DRAFT → READY_FOR_REVIEW → ACCEPTED | REJECTED | EXPIRED`；被接受的内容仅进入目标领域的草稿/候选状态，不直接成为签署事实。

**Agent/Skill/Tool 版本：** `DRAFT → CANDIDATE → VALIDATED → APPROVED → SUSPENDED | RETIRED`；`CREATED` 仅表示制品已生成，`VERIFIED` 必须有测试证据。

## 7. 业务规则

| ID | 规则 |
|---|---|
| BR-001 | Agent 是受控任务执行者，不具备医疗执业、签署或最终决策身份。 |
| BR-002 | 临床 AI 默认自治上限为 A1；结构化候选可到 A2，但必须由授权人员逐项确认并由领域内核执行。 |
| BR-003 | A4 自主诊断、处方、签署、治疗、执行和解除阻断一律 `DENY`。 |
| BR-004 | 每个用户任务只绑定一个主 Agent 和一个可理解目标；子 Agent 与 Skill 组合不得改变主目标或扩大范围。 |
| BR-005 | 前台统一入口不得形成通配权限；每次运行必须绑定当前机构、主体、患者/就诊或明确安全空上下文。 |
| BR-006 | 切换患者、就诊、岗位、机构、任务或页面关键对象后，旧 ContextLease、审批和候选立即失效。 |
| BR-007 | 事实性临床结论必须可回到具体来源和版本；证据不足时输出“无足够证据/未知”，不得补写。 |
| BR-008 | AIProposal 与临床事实、已签文书、已确认诊断、已生效医嘱和已完成任务分层保存。 |
| BR-009 | 接受候选不等于签署或执行；后续仍使用原业务权限、校验、签署和状态机。 |
| BR-010 | 硬规则、确定性 Tool 结果和 AI 建议必须视觉及数据分层；AI 不得降低硬规则严重度。 |
| BR-011 | Tool 必须采用强 Schema 和白名单；禁止向 Agent 暴露通用 SQL、通用 HTTP、任意文件或任意代码执行。 |
| BR-012 | Tool 执行端必须独立鉴权、校验患者/就诊和业务状态，不信任提示词或服务账户声称的权限。 |
| BR-013 | 有副作用 Tool 必须声明风险、幂等、超时、失败、补偿、核验和审批；缺一不得发布。 |
| BR-014 | 人工审批只覆盖页面展示的具体 payload；内容、上下文或版本变化后必须重新批准。 |
| BR-015 | 提案创建者不得独立最终批准高风险 Agent/Skill/Tool 或具体动作。 |
| BR-016 | Skill 必须声明输入输出、前后置、允许上下文/Tools、失败语义、资源预算、评测集、版本和责任人。 |
| BR-017 | Agent/Skill/Tool 的所有依赖锁定兼容版本；依赖变化使原批准失效并触发受影响用例重评。 |
| BR-018 | 专科差异通过 Skill Pack 扩展模板、字段、知识、规则和 Evals，不复制患者/就诊/文书/医嘱内核。 |
| BR-019 | 运行记录保存必要结构化轨迹、Tool 请求/结果、来源、版本、审批、反馈和最终状态，不要求保存或展示隐藏思维链。 |
| BR-020 | 输出被修改后，应分别保留 AI 原候选、人工差异和最终业务版本，不用最终文本反向伪装 AI 原输出。 |
| BR-021 | 模型、Agent、Skill、Tool 或用例策略均可独立紧急停用；停用不得阻断人工 EMR。 |
| BR-022 | 外部或社区 Skill/Tool 默认不受信，须完成来源、签名、许可证、秘密、敏感数据、依赖和执行能力检查。 |
| BR-023 | 生产效果不能只用调用量或采纳率判断；至少联合净复核时间、修改率、误报/漏报、严重错误、人工接管和安全事件。 |
| BR-024 | 影子结果不得写入临床事实、发送患者消息或产生任何副作用。 |
| BR-025 | 当前已验证的本地动作执行治理不得被描述为真实下游业务动作已验证；接入每个领域适配器后需单独端到端验收。 |
| BR-026 | L0 统一入口只负责上下文与路由；主 Agent 负责端到端任务结果；子 Agent 负责单一诊疗环节；Skill 和 Tool 不得被包装成拥有临床身份的 Agent。 |
| BR-027 | 只有同时具备独立目标、结构化输入输出、运行状态、预算、失败/恢复语义和独立 Eval 的工作单元才注册为子 Agent；否则应实现为 Skill 或 Tool。 |
| BR-028 | 一次用户任务只能有一个主 Agent 对最终 AIProposal、用户交互、失败说明和人工接管负责；子 Agent 不得绕过主 Agent 向临床事实层提交结果。 |
| BR-029 | 主 Agent 只能编排已批准 AgentComposition 中锁定版本的白名单子 Agent；禁止在生产运行中动态创建、下载或自我选择未批准子 Agent。 |
| BR-030 | 子 Agent 的有效权限、上下文、自治等级和预算必须分别小于或等于父运行；任何子 Agent 或兄弟 Agent 都不能扩大父级范围。 |
| BR-031 | 父运行必须定义总步数、时间、成本和 Tool 预算；每个子运行定义子预算；任一总预算耗尽时不得以启动新子 Agent 绕过限制。 |
| BR-032 | AgentComposition 必须声明关键/非关键子任务、部分失败、跳过、重试、取消传播和人工接管规则；无声明时任何子 Agent 失败均使父运行 `BLOCKED`。 |
| BR-033 | 子 Agent 的中间输出只能传给组合契约声明的下一节点；主 Agent 必须完成最终来源、患者一致性、Schema 和冲突验证后才能生成用户候选。 |
| BR-034 | 父子运行必须记录 root/parent/child 关系、具体版本、输入输出引用、状态、预算和错误；运行详情不得展示隐藏思维链或无必要病历正文。 |
| BR-035 | 子 Agent 独立 Eval 通过不等于主 Agent 组合通过；新增、删除、换序或升级任何关键子 Agent 均触发组合回归、受影响诊疗环节评测和重新批准。 |

## 8. 功能需求契约

以下每条需求中的“用户可见结果”均为可验收行为，不预设具体模型或编排框架。

### 8.1 入口、运行与审阅

| ID | 用户/价值与前置 | 用户可见结果 | 状态、异常、权限 | 关联 |
|---|---|---|---|---|
| FR-001 | 授权临床人员从任意工作域快速调用医助；前置为已登录且页面上下文可识别。 | 入口持续显示当前机构、角色、患者、就诊、任务和页面；无患者时明确显示安全空上下文；两次操作内进入任务。 | 切患者/岗位立即清空旧正文和候选；无权来源不显示存在性；AI 失败不遮挡主页面。 | BR-005/006/021；AC-001；NFR-001/005；RISK-001 |
| FR-002 | 用户用任务卡或自然语言表达意图；前置为存在已批准用例。 | 系统展示路由到的主 Agent、当前诊疗环节、将读取的数据类别、主要输出和自治等级；不确定意图时先澄清，不静默选择高风险能力。 | 无批准路由、目标超范围、多患者请求或诊疗环节不明确时阻止；用户可改选任务或退出。 | BR-001–005/026/029；AC-002；NFR-004/013；RISK-002/026 |
| FR-003 | 用户理解长任务并保持控制；前置为主 AgentRun 已创建。 | 展示主任务、诊疗阶段、已完成/待执行子任务、部分失败、预计是否需审批和停止/接管入口；不展示隐藏思维链。 | 取消父运行后不再创建子运行或调用 Tool；超时、预算或版本异常显示具体子阶段；仅安全步骤可恢复。 | BR-004/019/031–034；AC-003；NFR-002/004/014/015；RISK-003/027 |
| FR-004 | 用户核对 AI 事实；前置为结果进入 `READY_FOR_REVIEW`。 | 每个关键事实可展开来源对象、时间、版本和原文定位；冲突、缺失、迟到/更正来源单独显示。 | 来源不可用时不得标记已验证；跨患者来源触发 `BLOCKED`；来源权限实时重验。 | BR-006/007/010；AC-004；NFR-005/008；RISK-001/004 |
| FR-005 | 用户安全处理候选；前置为 AIProposal 未过期。 | 可接受、逐项修改、拒绝、复制到人工草稿或查看差异；系统记录理由和人工修改。 | 上下文变化则候选 `EXPIRED`；接受只进入领域草稿/候选，不越过签署或执行。 | BR-008/009/020；AC-005；NFR-008；RISK-005 |
| FR-006 | 用户在 AI 故障时继续工作；前置为人工流程可用。 | 超时、模型/Tool 不可用或紧急停用时，显示原因、已完成范围和“转人工”入口；已输入内容可安全保留。 | 不伪装部分结果为完整；重试不得重复副作用；人工核心任务保持可完成。 | BR-021/025；AC-006；NFR-003/006；RISK-003/006 |

### 8.2 P0 医疗主 Agent

| ID | 用户/价值与前置 | 用户可见结果 | 状态、异常、权限 | 关联 |
|---|---|---|---|---|
| FR-007 | 医生/护士希望快速掌握本次诊疗；前置为患者/就诊和可用只读来源。 | `ENCOUNTER_SUMMARIZER` 按问题、用药/过敏、近期结果、事件、未闭环任务生成带引用摘要和待确认问题。 | 部分数据为 `INCOMPLETE`；冲突并列不消解；只读且不生成诊断确认。 | BR-002/007；AC-007；NFR-007/008；RISK-004/007 |
| FR-008 | 医生/护士希望减少文书起草；前置为合法文书任务、模板版本和已确认输入。 | `DOCUMENT_DRAFTER` 生成结构化文书候选，逐段显示来源、未确认项、模板缺口和与当前草稿差异。 | 不虚构查体/设备/执行事实；模型失败回人工模板；不得自动保存为已签内容。 | BR-007–009/018；AC-008；NFR-007/008；RISK-007/008 |
| FR-009 | 医生/质控人员需要语义质控第二视角；前置为不可变文书版本和硬规则结果。 | `RECORD_QC` 分层输出遗漏、冲突、时序、复制和语义问题，定位证据、严重度建议和整改方向。 | 硬规则始终单列且严重度不可被 AI 降级；不直接改原文；低证据项标注不确定。 | BR-010；AC-009；NFR-007/008；RISK-009 |
| FR-010 | 医生/护士需要闭环异常结果；前置为已确认结果、危急值状态和任务数据。 | `RESULT_FOLLOWUP_COORDINATOR` 展示趋势、风险来源、已有处置、未闭环责任与任务候选。 | 危急值判定和状态来自确定性规则；任务仅为候选；来源缺失不推测已通知/已处理。 | BR-002/010/013；AC-010；NFR-007/008；RISK-010 |
| FR-011 | 会诊/随访协调人员需准备跨团队协同；前置为明确患者、目标团队和授权范围。 | `CARE_COORDINATOR` 输出会诊/转诊/交班摘要、目标、关键证据、未决问题和可预览任务计划。 | 发送、改约、建任务需原权限与审批；不代替责任医生确认结论；过期计划不得执行。 | BR-002/014；AC-011；NFR-005/008；RISK-011 |

### 8.3 Skill 产品化

| ID | 用户/价值与前置 | 用户可见结果 | 状态、异常、权限 | 关联 |
|---|---|---|---|---|
| FR-012 | 设计者和治理人员需要发现可复用能力；前置为候选 Skill 有稳定标识。 | Skill 能力卡展示用途、适用/禁用场景、角色、输入输出、数据范围、允许 Tools、失败语义、Evals、版本、负责人和状态。 | 必填缺失、模糊 Schema、无负责人/评测或不明许可证时不能进入 `CANDIDATE`。 | BR-016/022；AC-012；NFR-009；RISK-012 |
| FR-013 | Agent 以稳定契约执行 Skill；前置为具体版本已被用例批准。 | 运行前显示所选 Skill 和版本；输出符合 Schema，并区分 `COMPLETE`、`INCOMPLETE`、`NO_EVIDENCE`、`BLOCKED`、`FAILED`。 | 隐式 Tool、数据超范围、依赖漂移或 Schema 不合格立即阻止；历史运行可解析原版本。 | BR-016/017；AC-013；NFR-004/009；RISK-013 |
| FR-014 | 设计者组合多个 Skill；前置为输入输出和版本兼容。 | 管理界面展示依赖、数据流、权限交集、执行顺序、失败分支和最终输出；发布制品锁定解析结果。 | 循环、Schema 不匹配、权限扩大、无界递归/并行或副作用无补偿时阻止。 | BR-004/017；AC-014；NFR-009；RISK-013/014 |
| FR-015 | 专科团队扩展基础能力；前置为基础 Skill 提供扩展点。 | 专科 Skill Pack 可增加术语、字段、模板、知识、规则和 Evals，并声明与基础版本的兼容范围。 | 不得复制核心临床对象、关闭保护规则或携带任意代码；不兼容升级需新主版本。 | BR-018/022；AC-015；NFR-009；RISK-015 |

### 8.4 Tool 与动作治理

| ID | 用户/价值与前置 | 用户可见结果 | 状态、异常、权限 | 关联 |
|---|---|---|---|---|
| FR-016 | 治理人员需要理解 Tool 风险；前置为目标业务能力有受保护契约。 | Tool 能力卡展示用途、读写级别、输入输出、业务前置、权限、患者范围、幂等、超时、错误、补偿、审批和版本。 | 通用 SQL/HTTP、通配权限、秘密回显、无患者范围或副作用缺少幂等/核验时不得发布。 | BR-011–013；AC-016；NFR-005/009；RISK-016 |
| FR-017 | Agent 安全读取临床上下文；前置为 ContextLease 有效。 | 首批只读 Tool 提供就诊上下文、文书版本、观察结果、医嘱/执行、问题/过敏/用药和开放任务的结构化结果及来源引用。 | 每次调用重新鉴权；超范围字段不返回也不泄露存在性；部分失败显式标记。 | BR-005/006/012；AC-017；NFR-005/006；RISK-001/016 |
| FR-018 | Agent 保存候选而非修改事实；前置为输出通过 Schema 和来源验证。 | `create_document_proposal`、`create_task_proposal` 等仅创建独立 AIProposal，并返回可审阅引用和 payload hash。 | 不得写已签文书/已完成任务；重复请求返回同一候选；上下文过期拒绝。 | BR-008/013/020；AC-018；NFR-006/008；RISK-005/017 |
| FR-019 | 用户批准确需副作用的动作；前置为动作来自已批准用例且可预览。 | 审批页完整展示患者、对象、参数、证据、风险、差异、有效期；批准后由领域内核执行并返回真实业务结果。 | 自批、过期、payload 变化、撤权、重复回调或领域状态不合法均阻止；失败进入核验/对账而非伪装成功。 | BR-013–015/025；AC-019；NFR-005/006/010；RISK-017/018 |

### 8.5 Agent 目录、评估与运营治理

| ID | 用户/价值与前置 | 用户可见结果 | 状态、异常、权限 | 关联 |
|---|---|---|---|---|
| FR-020 | 治理人员管理主/子 Agent；前置为目标、角色、诊疗环节和用例明确。 | Agent 能力卡展示 `MAIN/SUB` 层级、父主 Agent、诊疗环节、单一目标、触发、上下文、Skills、Tools、自治等级、预算、审批点、停止/接管、输出、版本和负责人。 | 无限目标、无父级的生产子 Agent、通配 Tool、无预算/停止/人工接管或自治超边界时不能进入候选。 | BR-001–004/026/027；AC-020；NFR-009/013；RISK-002/019/024 |
| FR-021 | 医院只上线有证据的版本；前置为用例评测集、严重错误定义和审批角色就绪。 | 主 Agent、子 Agent 和组合分别经过契约、质量、权限、跨患者、提示注入、故障、模型切换、人工复核、影子和灰度门禁；结果形成 EvalEvidence。 | 单个子 Agent 通过不能替代组合测试；严重错误、安全样本、来源、Schema 或职责分离任一硬门禁失败则阻止；不能用总体均值掩盖关键亚组。 | BR-015/017/022–024/035；AC-021；NFR-007/010/011/015；RISK-008/020/029 |
| FR-022 | 治理/运维持续了解效果与风险；前置为统一运行事件和指标口径。 | 按机构、科室、用例、主 Agent、子 Agent、Skill/Tool/模型版本查看成功、部分完成、失败、接管、修改、误报漏报、延迟、成本和安全事件，并下钻父子授权轨迹。 | 观测盲区时高风险用例停止扩大；运营视图默认脱敏；不得展示隐藏思维链。 | BR-019/023/034；AC-022；NFR-011–014；RISK-020/021/027 |
| FR-023 | 临床用户反馈结果质量；前置为可定位 AgentRun/AIProposal。 | 支持标记有用、错误、缺失、来源错误、患者错误、过时、表达问题和拒绝原因；反馈进入评测队列并可查看处置状态。 | 反馈不得直接训练生产模型或修改规则；患者错误立即触发安全事件；敏感自由文本受控。 | BR-019/023；AC-023；NFR-005/011；RISK-001/021 |
| FR-024 | 管理员快速隔离问题组件；前置为有权限和事件责任链。 | 可按用例、主 Agent、子 Agent、Skill、Tool、模型/路由单独暂停，显示受影响诊疗环节、父级组合、人工替代、恢复门禁和审计。 | 停用子 Agent 后，依赖它的主 Agent 仅在存在已批准降级组合时继续；否则拒绝新运行；恢复须重新验证依赖与策略。 | BR-021/029/032/035；AC-024；NFR-003/011/015；RISK-006/020/030 |
| FR-025 | 医院按科室/角色管理准入和提醒负担；前置为 UseCasePolicy 已定义。 | 可配置入口、主动/被动模式、允许来源、版本、自治上限、提醒限频、预算、灰度人群和有效期，并预览最终生效策略。 | 低层策略不得放宽平台硬边界；冲突或无来源覆盖链时不能发布；在途实例绑定原策略版本。 | BR-002/003/017/021；AC-025；NFR-009/011；RISK-019/022 |
| FR-026 | 平滑迁移当前 6 个前台 Agent；前置为旧代码与新目录映射获批。 | 保留用户熟悉入口：`OPD_COPILOT` 路由摘要/文书/计划主 Agent 及环节子 Agent；`ORDER_SAFETY` 映射 SafetyReviewer；`RESULT_TRIAGE` 映射结果闭环主 Agent；`DOCUMENT_QC` 映射质控主 Agent；`CARE_COORDINATOR` 保留为主 Agent；`DIAGNOSIS_REVIEW` 标记实验。 | 未通过主/子/组合 Eval 的旧 Agent 不得因“已存在”自动批准；迁移保留历史运行解析，入口变化可回滚。 | BR-001–004/017/026–035；AC-026；NFR-009/011/013–015；RISK-019/023/029 |

### 8.6 主 Agent—子 Agent 分层与诊疗环节适配

| ID | 用户/价值与前置 | 用户可见结果 | 状态、异常、权限 | 关联 |
|---|---|---|---|---|
| FR-027 | 产品、临床和治理人员需要区分主 Agent、子 Agent、Skill 与 Tool；前置为能力目标可描述。 | 目录按 L0–L4 展示层级、父级、诊疗环节、独立状态/Eval 与权限摘要，并解释为何该能力是子 Agent 或 Skill。 | 不满足独立目标、状态、预算、恢复和 Eval 的候选不能注册为子 Agent；层级变更视为重大版本变化。 | BR-026/027；AC-027；NFR-009/013；RISK-024/025 |
| FR-028 | 临床用户在不同诊疗阶段获得正确能力；前置为页面、就诊类型和业务对象状态可识别。 | 系统展示当前诊疗环节、被选择的主 Agent 和计划调用的子 Agent；相同意图在门诊、急诊、住院、围术期、出院或归档时路由到不同环节组合。 | 环节不明确、跨域切换或在途状态冲突时先澄清/阻止；不得沿用旧工作域组合。 | BR-005/006/026/029；AC-028；NFR-005/013；RISK-001/026 |
| FR-029 | Agent 设计者配置可解释的父子编排；前置为主/子 Agent 版本已存在。 | AgentComposition 展示白名单子 Agent、触发条件、顺序/并行、输入输出映射、关键性、预算、部分失败、取消和人工接管；生成可读任务图。 | 循环、动态未批准节点、权限扩大、总预算缺失、无最终聚合或失败规则缺失时不能发布。 | BR-029–033；AC-029；NFR-009/013/014；RISK-025/027 |
| FR-030 | 主 Agent 安全派生子运行；前置为父 ContextLease 和组合有效。 | 每个子任务显示更小的数据范围、允许 Skill/Tool、子预算和状态；运行详情可追溯 root/parent/child。 | 子 Agent 请求超出父范围、版本不符或预算超限时拒绝；不得通过兄弟 Agent 间隐式传数扩大范围。 | BR-030/031/033/034；AC-030；NFR-005/013/014；RISK-027/028 |
| FR-031 | 用户控制多子任务运行并理解部分失败；前置为父运行已创建。 | 用户可取消整个任务或接管某环节；主 Agent 显示关键/非关键子任务、完整/部分/失败结果以及缺失对最终候选的影响。 | 父取消传播到未完成子运行；关键子任务失败使父 `BLOCKED`；仅批准组合允许非关键失败后形成显著 `PARTIAL` 结果。 | BR-028/031–034；AC-031；NFR-002/003/014；RISK-003/027/030 |
| FR-032 | 用户在诊前、分诊、接诊、每日查房、围术期和出院准备获得阶段化摘要；前置为 `ENCOUNTER_SUMMARIZER` 和对应子 Agent 获批。 | 主 Agent 按环节调用 PreVisit、TriageContext、ActiveEncounter、InpatientDaily、PerioperativeContext 或 DischargeReadiness 子 Agent，只汇总当前任务必要结果。 | 多环节请求拆为明确子任务；任何来源/患者不一致使相关子 Agent `BLOCKED`；不得由摘要子 Agent产生诊断或出院决定。 | BR-007/026–033；AC-032；NFR-005/007/008/014；RISK-004/026 |
| FR-033 | 医生/护士在不同文书节点获得对应草稿；前置为合法文书任务、模板和作者职责。 | `DOCUMENT_DRAFTER` 根据门诊、急诊、入院、首程、日常病程、查房、会诊、围术期、护理交班或出院节点调用对应子 Agent，并生成一个可逐段审阅的主候选。 | 子 Agent 不能混用文书类型或作者身份；缺关键事实的段落保持未确认；任一关键文书子任务失败回到对应人工模板。 | BR-008/009/026–033；AC-033；NFR-007/008/013/014；RISK-007/026 |
| FR-034 | 作者、质控和病案人员在书写中、签署前、运行和终末阶段获得不同质控；前置为对应不可变版本和规则结果。 | `RECORD_QC` 路由 WritingQC、PreSignQC、ActiveRecordQC、TerminalRecordQC 或 CorrectionConsistency 子 Agent；输出保留生命周期阶段、证据和责任。 | 不同阶段不得错误复用严重度/阻断语义；AI 子 Agent 不修改硬规则、原文或正式缺陷终态。 | BR-010/026–035；AC-034；NFR-007–009/014；RISK-009/029 |
| FR-035 | 临床团队按新结果、趋势、危急值、待回结果、随访或结果更正完成闭环；前置为权威结果/任务状态可读。 | `RESULT_FOLLOWUP_COORDINATOR` 调用对应结果子 Agent，聚合状态并在需要时生成一个任务计划候选。 | 危急值判定/通知/处置和结果确认来自确定性规则/业务状态；某子任务状态未知时主候选必须列出，不能自动完成任务。 | BR-010/013/026–035；AC-035；NFR-006–008/014；RISK-010/018/029 |
| FR-036 | 会诊、MDT、转科、出院、随访和任务交接由对应环节子 Agent 准备；前置为责任团队和目标明确。 | `CARE_COORDINATOR` 调用 ConsultPreparation、MDTBrief、TransferHandoff、DischargeTransition、FollowupCoordination 或 TaskReconciliation 子 Agent，输出可预览协同候选。 | 子 Agent 不伪造共识、交接或任务完成；发送/转派/关闭仍需原业务权限与审批；父任务汇总时保留各责任方未确认项。 | BR-009/013/026–035；AC-036；NFR-005/006/008/014；RISK-011/018/029 |

## 9. 验收标准

| ID | Given / When / Then |
|---|---|
| AC-001 | **Given** 用户在门诊患者 A 页面打开医助，**When** 切到患者 B 或安全空上下文，**Then** 患者 A 正文、建议、审批和可执行动作全部不可见且不可提交，入口显示新上下文。 |
| AC-002 | **Given** 用户输入超出批准用例或包含多个患者的模糊请求，**When** 系统路由，**Then** 不创建高风险运行，展示澄清或可选任务，不泄露患者存在性。 |
| AC-003 | **Given** AgentRun 正在调用多个只读步骤，**When** 用户取消，**Then** 后续 Tool 不再调用，运行变为 `CANCELLED`，已完成只读轨迹可审计且无候选自动落库。 |
| AC-004 | **Given** 摘要包含诊断、用药、结果或任务事实，**When** 用户点击来源，**Then** 可定位到同患者/就诊的具体对象、版本和时间；来源不可用项明确标记未验证。 |
| AC-005 | **Given** AIProposal 已生成，**When** 用户接受、修改或拒绝，**Then** 分别保留原候选、人工差异和处理结果；接受后仍停留在目标领域草稿/候选状态。 |
| AC-006 | **Given** 模型和 Agent 服务全部不可用，**When** 用户继续病历书写、医嘱、签署或任务处理，**Then** 原人工主链可完成，页面不把故障结果显示为完整答案。 |
| AC-007 | **Given** 某来源延迟或相互冲突，**When** 生成就诊摘要，**Then** 输出为 `INCOMPLETE`，并列冲突和缺失，不自动选择一个结论。 |
| AC-008 | **Given** 文书输入缺少查体或执行证据，**When** 生成草稿，**Then** 对应段落显示未确认/缺失，不生成虚构正常值、已执行事实或签名。 |
| AC-009 | **Given** 硬规则命中高严重度缺陷，**When** RecordQC 给出不同意见，**Then** 硬规则仍单独展示且严重度不降低，AI 仅以建议层呈现。 |
| AC-010 | **Given** 危急值已确认但通知状态缺失，**When** 结果闭环 Agent 运行，**Then** 显示危急值来源和“通知状态未知”，只生成任务候选，不声称已通知。 |
| AC-011 | **Given** 会诊任务计划已获预览，**When** 患者、目标团队或关键参数变化，**Then** 旧计划和批准过期，任何提交都要求重新确认。 |
| AC-012 | **Given** Skill 缺少失败语义、评测集或责任人，**When** 设计者提交候选，**Then** 发布被阻止并指出缺失字段。 |
| AC-013 | **Given** 已批准 Skill 的输出不符合 Schema 或依赖版本漂移，**When** 运行验证，**Then** 结果不进入 `READY_FOR_REVIEW`，运行标记 `BLOCKED` 并可追踪原因。 |
| AC-014 | **Given** Skill 组合存在循环、权限扩大或数据模式不匹配，**When** 解析依赖，**Then** 不能发布，管理界面显示具体冲突路径。 |
| AC-015 | **Given** 专科包尝试复制核心患者对象或关闭平台保护规则，**When** 提交验证，**Then** 静态门禁拒绝且不能通过普通例外绕过。 |
| AC-016 | **Given** Tool 暴露通用 SQL/HTTP、通配权限或缺少副作用幂等，**When** 提交发布，**Then** Tool 不能进入 `APPROVED`。 |
| AC-017 | **Given** 用户对某字段无权或 ContextLease 已撤销，**When** Agent 调用只读 Tool，**Then** 执行端拒绝或裁剪字段，不返回该数据也不泄露其存在性。 |
| AC-018 | **Given** 同一 run、proposal type 和 payload hash 重复请求，**When** 创建文书/任务候选，**Then** 返回同一 proposal，不产生重复记录或临床事实。 |
| AC-019 | **Given** 一个副作用动作已批准，**When** payload、权限、业务状态或有效期任一变化，**Then** 执行端拒绝；重复回调不重复副作用，失败显示真实状态并进入对账。 |
| AC-020 | **Given** Agent 定义缺少单一目标、预算、停止或接管条件，**When** 提交候选，**Then** 不能进入评估阶段。 |
| AC-021 | **Given** 候选版本在跨患者、提示注入、来源或严重临床错误测试中任一失败，**When** 申请灰度，**Then** 灰度被阻止且保留失败证据。 |
| AC-022 | **Given** 同一 Agent 切换模型、Skill 或 Tool 版本，**When** 管理员查看效果，**Then** 指标可按具体版本拆分，不把不同版本混成一个总体数字。 |
| AC-023 | **Given** 用户提交“患者错误”反馈，**When** 系统接收，**Then** 关联运行被标记安全事件，阻止继续使用相关候选，并进入责任队列。 |
| AC-024 | **Given** 某 Tool 被紧急停用，**When** 新运行或在途运行到达该 Tool，**Then** 新调用被拒绝、在途运行进入安全终态，人工流程仍可用且恢复需重新验证。 |
| AC-025 | **Given** 科室策略试图放宽平台 A4 禁止或跨患者规则，**When** 发布，**Then** 系统拒绝并展示平台保护规则来源。 |
| AC-026 | **Given** 当前 6 个前台 Agent 完成目录迁移，**When** 用户从旧入口发起任务，**Then** 可看到实际路由的主 Agent、诊疗环节和子 Agent 组合；实验能力不因旧状态自动进入生产批准。 |
| AC-027 | **Given** 一个候选能力只有单步模板转换且没有独立状态、预算或恢复，**When** 设计者尝试注册为子 Agent，**Then** 系统阻止并提示应建为 Skill；满足六项子 Agent 条件时才能进入候选。 |
| AC-028 | **Given** 用户分别在门诊接诊、急诊预检、住院查房和出院准备页面请求“总结患者”，**When** 系统路由，**Then** 显示同一主 Agent 下不同的环节子 Agent 组合，且切换工作域后旧组合失效。 |
| AC-029 | **Given** AgentComposition 含循环、未批准动态子 Agent、无总预算或未定义关键子任务失败策略，**When** 提交发布，**Then** 发布被阻止并展示具体节点和冲突路径。 |
| AC-030 | **Given** 父运行只允许当前住院就诊和只读结果，**When** 子 Agent 请求跨患者资料或任务写入 Tool，**Then** 子运行被拒绝并标记 `BLOCKED`，父运行不能把该输出用于候选。 |
| AC-031 | **Given** 一个主 Agent 包含两个关键和一个非关键子 Agent，**When** 关键子 Agent 失败，**Then** 父运行 `BLOCKED`；当非关键子 Agent 失败且组合明确允许时，父结果标记 `PARTIAL` 并显著列出缺失范围。 |
| AC-032 | **Given** 当前处于出院准备且存在未报告结果和未签文书，**When** EncounterSummarizer 运行，**Then** 只调用批准的 DischargeReadiness 等子 Agent，列出未闭环项，不输出“可以出院”的决定。 |
| AC-033 | **Given** 用户从主任查房任务发起文书草拟，**When** DocumentDrafter 编排，**Then** 使用 WardRoundNoteDrafter 而非普通日常病程子 Agent，并保留实际查房者、记录者和待补上级意见。 |
| AC-034 | **Given** 同一文书分别处于书写中、签署前和归档前，**When** RecordQC 运行，**Then** 路由到对应生命周期子 Agent，使用相应规则语义，且任何 AI 结果都不能降低硬规则严重度。 |
| AC-035 | **Given** 报告被更正且旧结果已被文书引用并生成随访任务，**When** ResultFollowupCoordinator 运行，**Then** CorrectedResultReconciler 列出受影响文书和任务候选，不覆盖已签文书、不删除历史任务。 |
| AC-036 | **Given** 转科交接仍有未完成医嘱、结果和护理任务，**When** CareCoordinator 运行，**Then** TransferHandoff 和 TaskReconciliation 子 Agent 分别输出交接与任务差异，由主 Agent 聚合并保留接收方未确认状态。 |

## 10. 非功能需求

| ID | 质量属性 | 要求与验收阈值 | 验证方式 |
|---|---|---|---|
| NFR-001 | 可达性 | 授权桌面页面的医助入口两次操作内可达率 100%。 | 全页面自动化与原型任务测试。 |
| NFR-002 | 可控性 | 所有非瞬时运行均提供可见状态和取消；取消后的新 Tool 调用数=0。 | 状态机、并发与取消测试。 |
| NFR-003 | 降级 | AI 全部不可用时，核心人工任务完成率=100%。 | 故障注入与端到端任务测试。 |
| NFR-004 | 契约完整性 | 进入用户审阅的结构化结果 Schema 通过率=100%；超预算运行不得继续。 | Contract tests、预算/超时测试。 |
| NFR-005 | 隐私与授权 | 跨患者上下文泄漏=0；越权字段返回=0；撤权后旧页面写/Tool 调用成功=0。 | 红队、属性测试、旧标签与撤权 E2E。 |
| NFR-006 | 幂等与恢复 | 相同幂等键的重复副作用=0；状态未知必须进入对账，不能标记成功。 | 重放、超时、重复回调、断点恢复测试。 |
| NFR-007 | 临床安全 | 批准评测集中的严重临床错误=0 才可灰度；具体非严重错误阈值由每个用例批准。 | 双人临床盲审和亚组报告。 |
| NFR-008 | 来源与可追溯 | 临床事实性陈述的可定位来源率=100%；AI 原候选、人工差异和最终版本可追溯率=100%。 | 来源链接、版本链和抽样审计。 |
| NFR-009 | 可维护性 | 已批准制品的 Agent/Skill/Tool/模型/策略版本解析率=100%；循环依赖和未锁版本进入生产数=0。 | 静态检查、依赖解析、历史回放。 |
| NFR-010 | 职责分离 | 高风险制品或动作的创建者自批成功数=0；影子运行副作用数=0。 | 权限矩阵和审批测试。 |
| NFR-011 | 可观测性 | 生产运行对主体、上下文、制品版本、状态、来源、审批、错误和最终结果的必要审计覆盖率=100%。 | 事件契约和审计抽样。 |
| NFR-012 | 性能与成本 | 各 Skill 的 p95 首响应、完成时间、Tool 数和成本阈值在影子基线后逐用例批准；未批准阈值前不得宣称达标。 | 负载、模型切换和预算报告。 |
| NFR-013 | 层级完整性 | 生产子 Agent 的父主 Agent、诊疗环节、目标、输入输出、预算、失败/恢复、Eval 和版本字段完整率=100%；孤立生产子 Agent=0。 | 目录静态检查和发布门禁。 |
| NFR-014 | 父子可追溯 | 每个子运行均能定位 root/parent run、主/子 Agent 版本、派生 ContextLease、输入输出引用、预算和终态，覆盖率=100%。 | 事件 Contract tests、审计抽样和历史回放。 |
| NFR-015 | 组合安全 | 未经批准的子 Agent 节点进入生产运行数=0；父级取消后新增子 Tool 调用数=0；关键子 Agent 失败后父候选进入审阅数=0。 | 组合解析、取消传播、故障注入和 E2E。 |

## 11. 数据、事件与隐私契约

### 11.1 最小数据字段

每次运行至少记录：`tenant_id`、`actor_id/role`、`patient_id`（安全空上下文可空）、`encounter_id`、`task/page context`、`clinical_stage`、`root/parent/run_id`、`agent_level`、`agent/version`、`composition/version`、`skills/versions`、`tools/versions`、`model route/version`、`policy/version`、`context lease/hash/expiry`、`run state`、`budget usage`、`source refs`、`proposal/approval/execution refs`、`errors`、`feedback` 和时间戳。

不得默认记录：模型隐藏思维链、无必要的完整病历副本、明文秘密、未经批准的原始音频或跨患者长期记忆。

### 11.2 核心事件

- `ai.run.created|started|blocked|cancelled|failed|completed`
- `ai.subrun.queued|started|complete|incomplete|no_evidence|blocked|failed|cancelled`
- `ai.composition.selected|partially_completed|blocked|replanned`
- `ai.context.issued|expired|revoked|mismatch_detected`
- `ai.tool.requested|denied|succeeded|failed|reconciled`
- `ai.proposal.created|reviewed|accepted|rejected|expired`
- `ai.approval.requested|approved|rejected|expired`
- `ai.execution.started|succeeded|failed|unknown`
- `ai.component.approved|suspended|retired|rolled_back`
- `ai.feedback.created|triaged|resolved`
- `ai.safety.cross_patient_detected|unauthorized_action_detected`

所有事件必须含机构、用例、运行、制品版本和必要业务对象引用；运营指标与患者级轨迹分权展示。

### 11.3 保留和训练边界

- ContextLease 与缓存按最短业务必要期限保存，过期后不可继续调用 Tool。
- 反馈不得默认进入外部模型训练；用于内部评测或微调须另有批准用途、去标识策略和数据版本。
- 原始音频、转写、提示和模型响应分别配置保留策略；P0 不默认采集环境音频。

## 12. 指标、实验与发布门禁

### 12.1 硬门禁

| 指标 | 阈值 |
|---|---:|
| 跨患者/越权上下文事件 | 0 |
| 未经批准的副作用 | 0 |
| 严重临床错误（批准评测集） | 0 |
| 临床事实性陈述可定位来源率 | 100% |
| AI 故障时人工核心任务完成率 | 100% |
| 影子运行副作用 | 0 |
| 重复副作用 | 0 |
| 未批准子 Agent 进入生产运行 | 0 |
| 关键子 Agent 失败后父候选进入审阅 | 0 |
| 父运行取消后新增子 Tool 调用 | 0 |

### 12.2 价值与负担指标

- 任务总时长、AI 等待时间、净人工复核时间、下班后文书时间。
- 草稿打开率、完成率、直接采纳率、采纳后修改率、拒绝/接管率。
- 质控误报、漏报、整改完成和硬规则影响。
- 结果闭环从确认到责任接受/完成的时长与逾期率。
- 主动提醒量、重复率、忽略率、每班次打断和高严重度漏报。
- 运行成功、Tool 失败、模型切换、成本、延迟和事件恢复时间。
- 诊疗环节路由准确率、人工改选率、子 Agent 启动/跳过/部分完成/失败率和父子预算放大系数。
- 各子 Agent 对主结果的有效贡献、重复输出、冲突率及因内部拆分增加的净等待/复核负担。

采纳率不得单独作为成功标准。收益目标在得到本地基线、科室差异和置信区间后由产品与临床共同批准。

### 12.3 首批实验

| 实验 | 版本/样本建议 | 比较与判定 |
|---|---|---|
| EXP-001 就诊摘要 | 300 份金标准回放；再 20 名临床人员、500 次就诊灰度 | 来源、严重遗漏/错误、净查阅时间、信任与接管；严重错误为 0 才扩大。 |
| EXP-002 文书草拟 | 10 名临床人员、200 例分层任务 | 人工模板 vs AI 草稿；测净复核时间、修改率、幻觉和签署前缺陷。 |
| EXP-003 语义质控 | 300 份含真实/合成缺陷的不可变文书 | 与硬规则和双人金标准比误报漏报；AI 不得改变硬规则。 |
| EXP-004 结果闭环 | 500 个结果/任务事件回放 | 测遗漏、错误责任、重复任务、闭环时长；危急值硬规则 100% 保留。 |
| EXP-005 协同摘要与任务 | 10 名用户、500 个会诊/转诊/随访任务 | 测摘要完整性、任务参数修改、过期计划执行和跨团队接管。 |
| EXP-006 诊疗环节路由与父子组合 | 覆盖门诊、急诊、住院查房、围术期、出院、随访和归档的合成/回放任务集 | 比较单主 Agent 与主/子组合的路由错误、严重遗漏、冲突、净等待、成本和接管；错误环节、未批准节点、权限扩大和关键失败漏传均为 0。 |

样本量为 `PLANNED` 的起始建议，不代表统计设计已批准；正式试验需按科室、任务和风险分层。

## 13. 风险、依赖与未决问题

### 13.1 风险登记

| ID | 风险 | 可能性/影响 | 缓解与门禁 |
|---|---|---|---|
| RISK-001 | 跨患者、撤权后旧上下文或来源泄漏 | 中/极高 | ContextLease、切换即失效、每 Tool 重鉴权、红队阈值 0。 |
| RISK-002 | 前台统一入口或主 Agent 演变为无边界万能 Agent | 高/高 | 主/子分层、诊疗环节显式路由、单一主目标、自治等级和 Tool 白名单。 |
| RISK-003 | 超时/重试造成用户卡住或重复动作 | 高/高 | 可取消、阶段状态、幂等、未知状态对账、人工降级。 |
| RISK-004 | 摘要遗漏、过时或错误引用 | 高/高 | 来源版本、完整性状态、冲突并列、验证 Agent、临床盲审。 |
| RISK-005 | 候选被误当作已确认事实 | 中/极高 | 数据/视觉分层、领域草稿状态、签署/执行不跳级。 |
| RISK-006 | 组件停用牵连核心 EMR | 中/高 | AI 与临床内核隔离、单组件停用、故障演练、人工主链 100%。 |
| RISK-007 | 文书幻觉、复制错误或过度冗长 | 高/高 | 未确认项、逐段来源、差异审阅、净复核时间和错误联合门禁。 |
| RISK-008 | 总体指标掩盖专科/语言/人群差异 | 中/高 | 分层评测、亚组报告、置信区间和科室灰度。 |
| RISK-009 | AI 质控稀释法规硬规则 | 中/极高 | 规则层级不可配置覆盖、独立视觉、硬规则回归。 |
| RISK-010 | Agent 把危急值或未闭环状态描述错误 | 中/极高 | 确定性规则/状态机为权威；Agent 只归纳并生成候选。 |
| RISK-011 | 会诊/随访任务发给错误团队或过期执行 | 中/高 | 可预览参数、逐次审批、上下文变化过期、真实结果核验。 |
| RISK-012 | Skill 变成难以审计的 Prompt 文件 | 高/中 | 强制能力卡、Schema、依赖、失败语义、Evals 和版本。 |
| RISK-013 | Skill 组合产生权限扩大或循环 | 中/高 | 静态依赖/数据流/权限交集检查，解析结果锁定。 |
| RISK-014 | 组合失败后产生半完成副作用 | 中/高 | P0 以候选为主；副作用需补偿、核验和状态对账。 |
| RISK-015 | 专科包分叉临床内核 | 中/高 | 扩展契约、禁止复制核心对象、架构评审门禁。 |
| RISK-016 | 通用工具或服务账户造成越权 | 中/极高 | 禁通用 SQL/HTTP、执行端鉴权、最小患者范围和秘密治理。 |
| RISK-017 | 审批内容与实际执行 payload 不一致 | 中/极高 | payload hash、有效期、执行前重验、不可变审批。 |
| RISK-018 | 本地状态成功但下游业务未真正完成 | 中/高 | 领域适配器端到端核验、`UNKNOWN` 状态、对账与补偿。 |
| RISK-019 | 旧目录迁移造成主/子 Agent 重复或边界模糊 | 高/中 | 前台兼容、主/子/Skill 判定、实验状态、历史版本保留。 |
| RISK-020 | 模型/依赖漂移后仍继续扩大 | 高/高 | 版本监控、门禁失效、暂停扩大、独立停用和回滚。 |
| RISK-021 | 反馈含敏感数据或被直接用于训练 | 中/高 | 结构化反馈、受控自由文本、用途审批和去标识。 |
| RISK-022 | 科室配置放宽平台安全边界 | 中/极高 | 平台保护规则优先，覆盖链预览，非法配置拒绝。 |
| RISK-023 | `DIAGNOSIS_REVIEW` 被误宣传为诊断 Agent | 中/极高 | `EXPERIMENTAL` 标签、仅证据一致性/缺口、研究和宣传审核。 |
| RISK-024 | 为追求数量把模板、规则或函数过度包装成子 Agent | 高/中 | 六项准入条件；不满足者归为 Skill/Tool；架构与产品双重评审。 |
| RISK-025 | 主/子层级和组合图过度复杂，研发与用户无法理解 | 中/高 | 限制层级深度、一次任务唯一主 Agent、默认折叠内部细节、组合静态检查。 |
| RISK-026 | 诊疗环节识别错误，调用了不适用的子 Agent | 中/高 | 页面/业务状态优先于自然语言推断；路由可见；跨域后组合失效；环节 E2E。 |
| RISK-027 | 子 Agent 级联失败、循环或预算放大 | 中/高 | DAG、父子预算、最大子运行数、取消传播、关键/非关键失败契约。 |
| RISK-028 | 子 Agent 或兄弟 Agent 间传递超范围数据 | 中/极高 | ContextLease 派生、显式输入输出映射、每节点重新鉴权、跨患者红队。 |
| RISK-029 | 单子 Agent 评测通过但组合后产生错误或冲突 | 高/高 | 独立 Eval + 组合 Eval + 诊疗环节端到端 Eval；任一关键变更重新批准。 |
| RISK-030 | 停用或升级子 Agent 导致主 Agent 静默缺能力 | 中/高 | 依赖影响分析、只允许批准降级组合、显著 `PARTIAL/BLOCKED`、回滚演练。 |

### 13.2 关键依赖

- 统一身份、角色、患者关系和业务状态的实时鉴权。
- Patient、Encounter、Document、Result、Order、Task 等稳定领域 API 和事件。
- 文书、结果、任务的不可变版本及来源深链。
- ContextLease、Tool Gateway、AIProposal、审批/执行和审计基础设施。
- 模型目录、路由、数据驻留、秘密、预算和故障转移策略。
- 临床金标准、错误分级、评审人员和科室灰度环境。
- 可表达主/子层级、AgentComposition、父子运行和诊疗阶段的目录、事件与评测基础设施。

### 13.3 未决问题

| ID | 问题 | 决策人 | 阻塞范围 |
|---|---|---|---|
| Q-001 | 首家试点医院与首批科室是什么？ | 产品负责人 + 医院临床负责人 | 样本、模板、术语、KPI、灰度 |
| Q-002 | P0 五个 Agent 的优先顺序和首发场景是门诊、住院还是病案质控？ | 产品委员会 | 版本切片和研发排期 |
| Q-003 | 允许使用哪些本地/私有/外部模型，临床数据是否可离院？ | AI 治理 + 安全/隐私 | 路由与数据边界 |
| Q-004 | 哪些指南、药品说明书、术语和机构制度可作为批准知识源？ | 临床/药学/病案负责人 | Skill 与引用 |
| Q-005 | 每个用例的 p95 延迟、成本和并发预算是多少？ | 产品 + 运维 + 财务 | NFR-012 与容量 |
| Q-006 | 任务、预约变更等首个真实领域适配器选哪个？ | 架构 + 业务负责人 | FR-019 端到端验证 |
| Q-007 | 反馈与运行轨迹的保留期限、去标识和内部评测用途如何批准？ | 隐私 + 数据治理 | FR-022/023 |
| Q-008 | 哪些科室/角色允许主动提醒，单班次打断阈值是多少？ | 临床负责人 | FR-025 |
| Q-009 | 33 个 P0 候选子 Agent 中，哪些作为真实独立子 Agent，哪些应下沉为 Skill？ | 产品 + 临床 + 架构 | FR-027/032–036 和首批 manifest |
| Q-010 | 首个试点优先验证哪条环节链：门诊接诊、住院查房、结果闭环还是出院准备？ | 产品委员会 + 试点科室 | AgentComposition、样本与排期 |
| Q-011 | 临床用户需要看到哪些子 Agent 名称和状态，哪些内部细节默认折叠？ | 产品设计 + 临床用户 | S003 信息架构与可用性测试 |

## 14. 追溯矩阵

| 本文需求 | 主 PRD 映射 | 下游交付 |
|---|---|---|
| FR-001–006 | FR-120–124；BR-025–031/117–125；NFR-050 | S003 全局入口/上下文/审阅；S004 降级与状态；S009 切患者、取消、故障 E2E |
| FR-007 | FR-031/035；FR-120–122 | S003 摘要卡与来源；S005 `ENCOUNTER_SUMMARIZER`；S009 摘要金标准 |
| FR-008 | FR-032/060；BR-040 | S003 文书差异审阅；S005 `DOCUMENT_DRAFTER`；S009 文书 Eval |
| FR-009 | FR-036/061；BR-041 | S003 硬规则/AI 分层；S005 `RECORD_QC`；S009 质控误报漏报 |
| FR-010 | FR-035/114；FR-123 | S003 结果闭环；S005 结果/任务 Tool；S009 危急值与任务状态 |
| FR-011 | FR-114/115/123 | S003 协同计划审批；S005 任务/预约适配器；S009 过期/错团队 E2E |
| FR-012–015 | FR-078–080/093；BR-065/066/083 | S004 Skill Pack 架构；S005 manifest/依赖；S009 Contract/Eval/供应链测试 |
| FR-016–019 | FR-077/123；BR-059–064 | S003 Tool 能力卡与动作预览；S005 Gateway/领域适配器；S009 鉴权/幂等/补偿 |
| FR-020–026 | FR-074–081/124；BR-054–067 | S003 治理中心；S004 发布/观测；S005 目录/事件；S009 发布门禁与演练 |
| FR-027–031 | FR-074–081/120–124；FLOW-14/15 | S003 主/子任务进度与接管；S004 父子边界；S005 Composition/父子运行；S009 组合、取消、预算和权限测试 |
| FR-032 | FR-031/035/082/083/120–122 | S003 分阶段摘要；S005 摘要子 Agent；S009 门急住/围术期/出院环节 Eval |
| FR-033 | FR-032/060/085–087/122 | S003 分文书任务草拟；S005 文书子 Agent；S009 作者、模板、来源和文书状态测试 |
| FR-034 | FR-036/061/100；FLOW-10 | S003 四阶段质控；S005 QC 子 Agent/Skill；S009 硬规则分层与生命周期 Eval |
| FR-035 | FR-035/103/114/123 | S003 结果闭环；S005 结果子 Agent/任务适配器；S009 危急值、更正和待回结果测试 |
| FR-036 | FR-086/087/114/115/123 | S003 会诊/交接/出院/随访；S005 协同子 Agent；S009 责任、过期、任务终态 E2E |
| NFR-001–015 | NFR-039–044/050/051 | S004 SLO/父子安全边界；S009 自动化、红队、负载、父子追溯和临床 Evals |

## 15. 建议版本切片

### R0：契约与治理基线

- 完成主/子 Agent、AgentComposition、Skill/Tool manifest、ContextLease、只读 Tool、AIProposal 和父子运行事件契约。
- 将当前 6 个前台 Agent 标记为兼容入口并建立主 Agent 映射；对 33 个 P0 候选子 Agent 完成“保留为子 Agent/下沉为 Skill”的评审。
- 用合成数据通过层级、DAG、父子预算、权限、跨患者、依赖、Schema、取消传播、部分失败、超时和紧急停用门禁。

### R1：只读摘要 + 文书候选

- 从一条真实诊疗链选择首批子 Agent，上线 `ENCOUNTER_SUMMARIZER` 与 `DOCUMENT_DRAFTER` 的父子编排影子/小范围灰度。
- 完成来源深链、差异审阅、人工模板降级和本地基线。
- 不接真实副作用 Tool。

### R2：质控 + 结果/协同任务候选

- 上线 `RECORD_QC`、`RESULT_FOLLOWUP_COORDINATOR`、`CARE_COORDINATOR`。
- 打通第一个真实领域适配器，完成审批、执行、业务结果核验和对账 E2E。
- 基于实验结果批准或收窄用例，而非按调用量自动扩大。

### R3：第二波 Skill

- 在 P0 硬门禁持续通过后，再评估 SafetyReviewer、编码、患者消息、环境听写和研究匹配。

## 16. Definition of Ready / Done

### 16.1 进入原型和 HLD 前（Ready）

- P0 场景、角色、输出和自治等级获得产品与临床负责人确认。
- Q-001–004 至少形成首个试点的临时决策。
- 每个 P0 主 Agent 及首批子 Agent 的诊疗环节、输入来源、关键性、禁用条件、严重错误和人工降级路径明确。
- Q-009–011 对首条试点环节链形成临时决策。
- Tool 被分类为只读、候选或副作用；副作用标出领域责任人。

### 16.2 宣称功能完成前（Done）

- 需求、原型、API/Schema、数据、权限、状态、事件和 AC 可双向追溯。
- AC-001–036 与 NFR 硬门禁通过；真实领域动作需端到端验证，不能只验证状态表。
- 临床评测含错误分级、亚组、双人复核、版本和适用范围。
- 影子/灰度、停用、回滚、故障、人工接管和事件处置均有证据。
- 用户文案始终使用“候选、草稿、建议、未确认”，不宣传为自主诊疗能力。

## 17. 交接说明

- **S003 原型**：优先产出全局上下文条、诊疗环节/主 Agent 路由确认、子任务进度与部分失败、运行/取消/接管、来源抽屉、候选差异、动作审批、治理能力卡和紧急停用界面。
- **S004 HLD**：确认统一入口、主 Agent、子 Agent、Skill、Tool 与临床内核的边界，以及 ContextLease 派生、父子预算、Tool Gateway、审批/执行、发布和观测边界。
- **S005 LLD**：为 P0 主/子 Agent、AgentComposition、Skill/Tool 输出 manifest、JSON Schema、错误码、父子状态、事件、幂等/补偿与领域适配器契约。
- **S009 测试/Evals**：从 AC-001–036 生成需求测试、权限/跨患者红队、组合 Contract tests、取消传播、父子预算、故障恢复、诊疗环节金标准和灰度监测方案。

本文状态为 `CREATED`，不表示产品、临床、安全或架构已批准；完成联合评审和决策记录后才能进入 `VERIFIED` 或正式版本基线。
