# 验收资产

本目录保存已经纳入版本基线的浏览器验证证据，不参与应用运行：

- `playwright-ci/`：全路由审计 JSON 与失败定位截图。
- `playwright/`：阶段性页面截图和历史浏览器审计脚本。

新生成的临时缓存、浏览器用户目录和构建产物仍应由 `.gitignore` 排除。需要引用验收结果时，应优先链接最终命名的审计文件，并在 `docs/process/testing/` 中记录对应门禁结论。
